<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"
	xmlns:content="http://purl.org/rss/1.0/modules/content/"
	xmlns:wfw="http://wellformedweb.org/CommentAPI/"
	xmlns:dc="http://purl.org/dc/elements/1.1/"
	xmlns:atom="http://www.w3.org/2005/Atom"
	xmlns:sy="http://purl.org/rss/1.0/modules/syndication/"
	xmlns:slash="http://purl.org/rss/1.0/modules/slash/"
	>

<channel>
	<title>Oracle &#8211; Sunvisor Lab. Ext JS 別館</title>
	<atom:link href="/tag/oracle/feed" rel="self" type="application/rss+xml" />
	<link>/</link>
	<description>Ext JS 関連の話題を取り上げます。</description>
	<lastBuildDate>Wed, 28 Sep 2011 00:47:02 +0000</lastBuildDate>
	<language>ja</language>
	<sy:updatePeriod>
	hourly	</sy:updatePeriod>
	<sy:updateFrequency>
	1	</sy:updateFrequency>
	<generator>https://wordpress.org/?v=6.1</generator>
	<item>
		<title>UbuntuにPDO-OCIを設置してPXを使えるようにする</title>
		<link>/393</link>
		
		<dc:creator><![CDATA[sunvisor]]></dc:creator>
		<pubDate>Wed, 28 Sep 2011 00:22:03 +0000</pubDate>
				<category><![CDATA[xFrameworkPX]]></category>
		<category><![CDATA[Oracle]]></category>
		<category><![CDATA[PDO]]></category>
		<guid isPermaLink="false">/?p=393</guid>

					<description><![CDATA[このエントリは，ほぼ完全に僕のための構築メモです。Ubuntu に Oracle のクライアントを入れて，PDOから使えるようにして，PXの開発環境を整えます。 Oracle Instant Client のインストール [&#8230;]]]></description>
										<content:encoded><![CDATA[<p>このエントリは，ほぼ完全に僕のための構築メモです。Ubuntu に Oracle のクライアントを入れて，PDOから使えるようにして，PXの開発環境を整えます。<span id="more-393"></span></p>

<h2>Oracle Instant Client のインストール</h2>

<p>https://help.ubuntu.com/community/Oracle%20Instant%20Client
を参考にインストール。</p>

<div>[bash]
    $ sudo apt-get install apache2
    $ sudo apt-get install php5 libapache2-mod-php5
    $ sudo service apache2 restart
[/bash]</div>

<p><a href="https://help.ubuntu.com/community/Oracle%20Instant%20Client">https://help.ubuntu.com/community/Oracle%20Instant%20Client</a>
からrpmファイルをダウンロードする。</p>

<ul>
<li>oracle-instantclient11.2-basic-11.2.0.2.0.i386.rpm</li>
<li>oracle-instantclient11.2-devel-11.2.0.2.0.i386.rpm</li>
<li>oracle-instantclient11.2-sqlplus-11.2.0.2.0.i386.rpm</li>
</ul>

<p>alien が必要なので入ってなければインストール。</p>

<div>[bash]
$ sudo apt-get install alien
[/bash]</div>

<p>alienを使うとrpmからインストールできるらしいです。</p>

<div>[bash]
    $ sudo alien -i oracle-instantclient11.2-basic-11.2.0.2.0.i386.rpm 
    $ sudo alien -i oracle-instantclient11.2-devel-11.2.0.2.0.i386.rpm 
    $ sudo alien -i oracle-instantclient11.2-sqlplus-11.2.0.2.0.i386.rpm 
[/bash]</div>

<p>basicのインストールは結構時間がかかりました。</p>

<div>[bash]
    $ sudo gvim /etc/ld.so.conf.d/oracle.conf
[/bash]</div>

<p>次の1行を追加。</p>

<div>[text]
    /usr/lib/oracle/11.2/client/lib/
[/text]</div>

<p>ldconfigを実行する。</p>

<div>[bash]
    $ sudo ldconfig
[/bash]</div>

<p>環境変数の指定。</p>

<div>[bash]
$ sudo gvim /etc/profile.d/oracle.sh
[/bash]</div>

<div>[text]
    export ORACLE_HOME=/usr/lib/oracle/11.2/client
    export LD_LIBRARY_PATH=/usr/lib/oracle/11.2/client/lib
[/text]</div>

<div>[bash]
$ . /etc/profile.d/oracle.sh 
[/bash]</div>

<p>Oracle Homeディレクトリとincludeディレクトリの位置確認。</p>

<div>[bash]
    $ ls $ORACLE_HOME
    bin  lib

    $ ls -d /usr/include/oracle/*/client
    /usr/include/oracle/11.2/client
[/bash]</div>

<p>シンボリックリンクの追加。</p>

<div>[bash]
    $ sudo ln -s /usr/include/oracle/11.2/client $ORACLE_HOME/include
[/bash]</div>

<h2>PDO-OCIのインストール</h2>

<p><a href="http://www.sunvisor.net/node/406">http://www.sunvisor.net/node/406</a></p>

<p>を参考にインストール。
基本的には同じ操作をします。
違う点は，iniファイルの場所は，/etc/php.d ではなく /etc/php5/conf.d であること
など，Centとubuntuによる違い。</p>

<h2>xFrameworkPXを使う準備</h2>

<p>UserDir/Rewriteモジュールを有効化します。ubuntuの場合はこうしたコマンドがあるのですね。</p>

<div>[bash]
    $ sudo a2enmod userdir
    $ sudo a2enmod rewrite
[/bash]</div>

<p>UserDirの設定。後ほど関係してきます。</p>

<div>[text]
&lt;IfModule mod_userdir.c&gt;
        UserDir &quot;/home/sunvisor/devsite/*/public_html&quot;

        &lt;Directory &quot;/home/sunvisor/devsite/*/public_html&quot;&gt;
                AllowOverride All
                Options Indexes FollowSymLinks Includes ExecCGI
                &lt;Limit GET POST OPTIONS&gt;
                        Order allow,deny
                        Allow from all
                &lt;/Limit&gt;
                &lt;LimitExcept GET POST OPTIONS&gt;
                        Order deny,allow
                        Deny from all
                &lt;/LimitExcept&gt;
        &lt;/Directory&gt;
&lt;/IfModule&gt;
[/text]</div>

<p>Apache再起動。</p>

<div>[bash]
$ sudo service apache2 restart
[/bash]</div>

<p>xFrameworkPXを取得</p>

<div>[bash]
$ cd ~/gitrep
$ git clone https://github.com/xenophy/xFrameworkPX
[/bash]</div>

<p>開発サイト作成。$HOMEの下にdevsiteディレクトリを作成し，
そのディレクトリのサブフォルダがプロジェクトになる。
ブラウザからは，http://localhost/~hoge でアクセスする。
先ほどのUserDirの設定が生きてくる。</p>

<div>[bash]
$ mkdir devsite
[/bash]</div>

<p>PXサイトを初期作成するスクリプト</p>

<div>[bash]
    #!/bin/sh
    PXLib=/home/sunvisor/gitrep/xFrameworkPX
    mkdir $1
    ln -s $PXLib/bindtransfer/ $1/bindtransfer
    ln -s $PXLib/library/ $1/library
    cp -r $PXLib/behaviors/ $1/behaviors
    cp -r $PXLib/configs/ $1/configs
    cp -r $PXLib/console/ $1/console
    cp -r $PXLib/controllers/ $1/controllers
    cp -r $PXLib/locales/ $1/locales
    cp -r $PXLib/modules/ $1/modules
    cp -r $PXLib/public_html/ $1/public_html
    cp -r $PXLib/templates/ $1/templates
    cp -r $PXLib/tests/ $1/tests
    cp -r $PXLib/webapp/ $1/webapp
    ln -s /home/sunvisor/ext-4.0.2a $1/webapp/ext4
    mkdir $1/cache
    chmod 777 $1/cache
    mkdir $1/logs
    chmod 777 $1/logs
    sed -i &quot;s/xFrameworkPX/&quot;$1&quot;/g&quot; $1/public_html/.htaccess
[/bash]</div>

<p>このスクリプトを叩くと，PXのプロジェクトが作られます。</p>
]]></content:encoded>
					
		
		
			</item>
	</channel>
</rss>
