<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"
	xmlns:content="http://purl.org/rss/1.0/modules/content/"
	xmlns:wfw="http://wellformedweb.org/CommentAPI/"
	xmlns:dc="http://purl.org/dc/elements/1.1/"
	xmlns:atom="http://www.w3.org/2005/Atom"
	xmlns:sy="http://purl.org/rss/1.0/modules/syndication/"
	xmlns:slash="http://purl.org/rss/1.0/modules/slash/"
	>

<channel>
	<title>Sencha Touch &#8211; Sunvisor Lab. Ext JS 別館</title>
	<atom:link href="/tag/sencha-touch/feed" rel="self" type="application/rss+xml" />
	<link>/</link>
	<description>Ext JS 関連の話題を取り上げます。</description>
	<lastBuildDate>Fri, 23 Oct 2015 09:12:43 +0000</lastBuildDate>
	<language>ja</language>
	<sy:updatePeriod>
	hourly	</sy:updatePeriod>
	<sy:updateFrequency>
	1	</sy:updateFrequency>
	<generator>https://wordpress.org/?v=6.1</generator>
	<item>
		<title>Modern ツールキットでのデータバインディング &#8211; Sencha ブログより</title>
		<link>/859</link>
		
		<dc:creator><![CDATA[sunvisor]]></dc:creator>
		<pubDate>Fri, 23 Oct 2015 09:12:43 +0000</pubDate>
				<category><![CDATA[Ext JS]]></category>
		<category><![CDATA[Sencha Touch]]></category>
		<category><![CDATA[ExtJS6]]></category>
		<guid isPermaLink="false">/?p=859</guid>

					<description><![CDATA[近頃、Sencha 本家のブログでは技術資料的な記事が少し少なくなっていましたが、先日久々に技術的な記事が掲載されました。 ご存じのように、Ext JS 6 では、以前の Ext JS (Version 5まで) と、S [&#8230;]]]></description>
										<content:encoded><![CDATA[<p>近頃、Sencha 本家のブログでは技術資料的な記事が少し少なくなっていましたが、先日久々に技術的な記事が掲載されました。</p>

<p>ご存じのように、Ext JS 6 では、以前の Ext JS (Version 5まで) と、Sencha Touch という2つのフレームワークがマージされました。
これまで、Ext JS のユーザーが Ext JS 6 へ移行する記事は、いくつか見られたのですが、Sencha Touch ユーザーに向けた、Ext JS 6 の解説記事はありませんでした。</p>

<p>この記事では、Ext JS 6 の ViewModel を modern ツールキットで使う方法が書かれています。ツールキットとは、Ext JS 6 のビジュアルな部分を受け持つところで、現在は、classic と modern の2つのツールキットが存在します。
modern ツールキットは、Sencha Touch の流れをくむツールキットです。</p>

<p>ViewModel は、Ext JS 5 で導入された、MVVM あるいは M-V-VM-VC という新しいアーキテクチャで採用された、ビューに関連してデータを保持するクラスです。
Sencha Touch だけをやってきた方には馴染みのない概念だと思いますが、これを利用することで、非常にアプリケーションを作成しやすくなります。</p>

<p>ではご覧ください。
<a href="https://www.sencha.com/blog/data-binding-in-the-ext-js-modern-toolkit/">Data Binding in the Ext JS Modern Toolkit | Sencha</a>
の翻訳です。
<span id="more-859"></span></p>

<h3>はじめに</h3>

<p>Ext JS 6 で &#8220;ツールキット&#8221; というコンセプトが導入されました。
これは、(主とする)フレームワークのビジュアルコンポーネントを指定するものです。
以前の Ext JS でのアプリケーションのビルドは、バージョン6では、&#8221;classic&#8221; ツールキットを使ったものにアップグレードし、Sencha Touch アプリケーションは、&#8221;modern&#8221; ツールキットが使われます。
双方のアプリケーションファミリーにとっての大きな利点は、共通した <code>core</code> パッケージが使えることです。
これは、ユニバーサルアプリケーションを構築する開発者が使えるというだけでなく、Sencha Touch アプリケーションを modern ツールキットにアップグレードする際に、多くの新機能 (特に重要なのは ViewModel とデータバインディング) が使えるようになります。</p>

<p>これらの機能をまだ見たことがない方は、
<a href="http://docs.sencha.com/extjs/6.0/application_architecture/view_models_data_binding.html">View Models and Data Binding Guide</a>
をご覧になると良いでしょう。
これにより、データバインディングという概念に関する強固な土台ができることでしょう。
この記事では、modern ツールキットにおけるデータバインディングの働きに商店を宛、いくつかの便利なテクニックを紹介しましょう。</p>

<h3>ViewModel</h3>

<p><code>Ext.app.ViewModel</code> くらすは、データバインディングを可能にする要となります。
ViewModel は、<code>Ext.data.Model</code> と似ていますが、data をフィールドのフラットなセットとして扱うのではなく、オブジェクトとプロパティの階層として扱い、階層内の変更を監視する為の <code>bind()</code> メソッドが提供されます。</p>

<h3>ViewModel の階層</h3>

<p>ViewModel の階層は、JavaScript のプロトタイプチェーンのように動作します。
子コンポーネントのViewModel は、親の ViewModel の中に含まれる全ての情報にアクセスできます。
孫やひ孫が、親子の関係を遡って、上位レベルのコンポーネントデータにアクセスできます。</p>

<p>次のサンプルでは、子アイテムが親の ViewModel にアクセスできることを示します。
また、
チェーンの中にプロパティを追加したり、非破壊的にそれらを変更することができます。
バインディングは、常にチェーンを上に向かって検索し、
値が見つかったらそれが返されます。</p>

<p><a href="https://fiddle.sencha.com/#fiddle/v3q">この Fiddle をご覧ください</a></p>

<p>上記のコードは、次の出力を返します。</p>

<p><em>The Don is Vito Corleone but I also know Genco Abbandando\
 The new Don is Sonny Corleone\
 Wait he’s not dead! Long live Vito Corleone!</em></p>

<h3>ViewModel のライフサイクル</h3>

<p>modern ツールキットでは、ViewModel の階層は、コンポーネント生成の間に確立されます。
これは単純ですが、ViewModel の親は、あとで変更できない関係であるということを理解するために不可欠な概念です。
この理由で、子要素を生成する際には、インスタンスを生成して手動で追加するのではなく、コンフィグを使うことを推奨します。</p>

<p>例えば、ユーザーが<code>Ext.create</code> でボタンを生成する場合、ボタンは、ViewModel の親のチェーンなしに生成されます。
このボタンがあとでコンテナーに追加されても、チェーンが再接続されることはありません。</p>

<h3>バインディング</h3>

<p>データバインディングは、<code>Ext.Widget</code> (<code>Ext.Component</code> の基底クラス) の機能で、ViewModel の変更を監視します。
変更はその後、コンポーネントのコンフィグに、コンフィグのセッターメソッドによって渡されます。
ViewModel の値が変更されたときには、その値にバインドされたすべてのコンフィグのセッターメソッドが新しい値で呼び出されます。</p>

<h3>Two-Way Binding</h3>

<p>すべてのバインディングは、Ext JS や Sencha Touch の開発者にはおなじみの、標準のセッターやアップデータのワークフローを通して動作します。
単方向のバインディングでは、コンフィグのセッターは、ViewModel の値が変更される度に呼び出されます。
これは、双方向バインディングでも、アップデータがインターセプトされる以外は同様です。
コンフィグの値が変更されアップデータに渡されたとき、
ViewModel のバインドされた値も変更されます。
更新する他のすべてのバインド値を順番に変更します。</p>

<p>コンフィグで双方向バインディングを定義する際には、便利な <code>twoWayBindable</code> コンフィグを使います。
普通は、このコンフィグは内部的に使われますが、
合致するコンフィグプロパティを持つカスタムビューやコンポーネントを書く際に便利です。
例えば、<code>Ext.field.Text</code> の定義では、<code>value</code> コンフィグを次のように双方向バインディングとして定義しています。</p>

<div>[js]
    twoWayBindable: {
        value: 1
    }
[/js]</div>

<p>次のサンプルでは、スピナーフィールドでの双方向バインディングで、簡単に formulas と連携できることを示します。</p>

<p><a href="https://fiddle.sencha.com/#fiddle/v3p">この Fiddle をご覧ください</a></p>

<p>スピナーフィールドの値が変更されると、
そのアップデータは、ViewModel にフックされます。
このトリガーで、下のコンテナーのバインディングがhtml コンフィグのセッターを呼び出します。
これは <code>Ext.field.Spinner</code> が、<code>value</code> コンフィグへの変更をを通知し、双方向バインディングが可能に設定されている <code>Ext.field.Text</code> を基底クラスとしているから、動作するのです。</p>

<h3>双方向バインディングを無効にする</h3>

<p>To disable the two-way binding in the above example, we could replace
this piece of code:
上記のサンプルで双方向バインディングを無効にするには、
一部のコードを修正します。</p>

<div>[js]
    bind: &#8216;{people}&#8217;
[/js]</div>

<p>上記の部分を次のように変更します。</p>

<div>[js]
    bind: {
        value: {
            bindTo: &#8216;{people}&#8217;,
            twoWay: false  // a bind option
        }
    }
[/js]</div>

<p>上記のコードはバインディングのいくつかの側面を示しています。
最初のケースでは、(スピナーの &#8220;value&#8221; である) <code>defaultBindProperty</code> に頼りました。
<code>bind</code> が単なる文字列のとき、<code>bind</code> オプションは、デフォルト値にセットされます。カスタムの <code>bind</code> オプションを指定するには、明示的に <code>bindTo</code> プロパティを使い、追加の属性を指定してバインディングを指定します。</p>

<p>このサンプルで双方向バインディングを無効にすると、スピナーを変更しても、コンテナーの html は変更されなくなることがわかるでしょう。</p>

<h3>発行 (Publishing)</h3>

<p>他のコンポーネントから利用したいコンポーネントのコンフィグは、ViewModel に発行することができます。
<code>publishes</code> コンフィグは、<code>twoWayBindable</code> と同様に、アップデータをフックしてコンフィグの値を ViewModel にプッシュします。
バインディングと違って、発行される値には、ViewModel で変更されたときに呼び出されるセッターがありません。</p>

<p>より重要なことに、データはViewModel 内に置かれるのではなく、発行されたコンフィグの値は、コンポーネントの名前を参照して名付けられたオブジェクトの中に保存されます。
多くのコンポーネントには、デフォルトの <code>publishes</code> コンフィグがあり、単純に、コンポーネントに <code>reference</code> コンフィグを設定することによってこれらの値は、ViewModel からアクセスできます。</p>

<p>list を使う時に、他のコンポーネントがリストの中のどのアイテムが選択されているかを知りたいような時にこれは、特に便利です。</p>

<p>次のサンプルでは、<code>Ext.dataview.DataView</code> には <code>selection</code> プロパティを発行する<code>Ext.mixin.Selectable</code>がミックスインされていることを使います。
これはフレームワークに組み込まれているので、<code>Ext.dataview.List</code> のインスタンスは、選択されたアイテムに他のコンポーネントがアクセスできます。</p>

<p><a href="https://fiddle.sencha.com/#fiddle/v3r">この Fiddle をご覧ください</a></p>

<h3>Forms and Fields</h3>

<p>modern ツールキットでは、フォームのフィールドは、最初からデータバインディングが動作するように設定されています。
<code>Ext.field.Text</code> を継承したすべてのコンポーネントは、value を ViewModel に発行し、双方向バインディングするように設定されています。
チェックボックスでは、<code>checked</code> コンフィグが発効され、これもまた双方向バインディングと連結されています。
スライダーも、双方向バインディングが有効で、単一または複数の値が発行されます。</p>

<p>次のサンプルでは、複数のフォーム要素を接続します。
セレクトフィールドでフォームの情報をフィルタさせ、チェックボックスでイメージのクラスをコントロールします。
<code>Ext.field.Checkbox</code> は、<code>checked</code> プロパティを発行するので、
そこに接続するためには、<code>'{checkboxReferenceName.checked}'</code> を使います。</p>

<p><a href="https://fiddle.sencha.com/#fiddle/v3u">この Fiddle をご覧ください</a></p>

<p>上記のサンプルでは、
1つの値を ViewModel の値に直接バインドすることで、
いくつかの視覚的結果を得ることができることがわかると思います。
例えば、次の行を</p>

<div>[js]
userCls: &#8216;{friend.checked:pick(&quot;&quot;, &quot;friend&quot;)}&#8217;,
[/js]</div>

<p>このように変更してみましょう。</p>

<div>[js]
userCls: &#8216;{person.value.friend:pick(&quot;&quot;, &quot;friend&quot;)}&#8217;,
[/js]</div>

<h3>リスト</h3>

<p><code>Ext.dataview.List</code> など <code>Ext.data.DataView</code> を継承したクラスを使う時には、
データバインディングは、<code>itemConfig</code> プロパティを使ってアイテム事に有効にできます。
レコードデータは、このコンフィグの中で bind ステートメントで <code>record</code> オブジェクトを使うことで、利用可能です。</p>

<p>例えば、&#8221;propertyName&#8221; というプロパティを参照するには、<code>'{record.propertyName}'</code> にバインドします。
欠くアイテムは、自身の ViewModel を生成して、その中に自身の formulas やバインドできるプロパティを生成できます。</p>

<p>次のサンプルでは、アイテムの ViewModel にカスタムの formula を使って、
計算を実行しています。
また、リストのストア内のレコードにバインドして、
各リストアイテムのコンポーネントの内側のテンプレートを設定しています。
最後に、ストアのデータをセレクトフィールドにバインドしています。</p>

<p><a href="https://fiddle.sencha.com/#fiddle/v6g">この Fiddle をご覧ください</a></p>

<h3>グリッド</h3>

<p>グリッドのデータバインディングは、非常にパワフルで各セルがレコードデータにアクセスできます。
グリッドは、親クラスの <code>Ext.dataview.List</code> のように、グリッドの <code>itemConfig</code> 内の <code>viewModel</code> コンフィグの設定で、行の中の全てのセルにレコードをバインドできます。</p>

<p>例えば、 <code>Ext.grid.cell.Widget</code> をセル内で使う時には、そのコンポーネントにレコードデータをバインドできます。
他によくあるグリッドのバインディングの例としては、セルの <code>innerCls</code> に接続することもあります。
そうすると、レコードからの値の組み合わせを元にして、セルのスタイルを設定できます。</p>

<p>次のサンプルでは、
双方の使用例を示します。
ボタンコンポーネントをグリッドセルの中に生成して、pick フォーマッタで条件による書式を設定しています。</p>

<p><a href="https://fiddle.sencha.com/#fiddle/v8u">この Fiddle をご覧ください</a></p>

<h3>まとめ</h3>

<p>ViewModel とデータバインディングのパワーは、modern ツールキットで本領を発揮します。
Config システムに依存しているおかげで、ほとんどのコンフィグプロパティは、バインドリクエストの応答します。
通常の用途でアプリケーションを素早く稼働できるようにするために、共通のコンポーネントは、最初から <code>twoWayBindable</code> とコンフィグの発行を利用できます。
データバインディングは、アプリケーションを構築する方法を基本的に変えます、そして、テキストフィールドから大きなグリッドまですべてはそれらに影響を及ぼすことができます。
好きなようにアプリケーションを書くために、データバインディングがあなたの開発者としてよく使うツールになると思います。</p>
]]></content:encoded>
					
		
		
			</item>
		<item>
		<title>Model/StoreとProxyの関係</title>
		<link>/646</link>
					<comments>/646#comments</comments>
		
		<dc:creator><![CDATA[sunvisor]]></dc:creator>
		<pubDate>Wed, 05 Dec 2012 20:08:46 +0000</pubDate>
				<category><![CDATA[Ext JS]]></category>
		<category><![CDATA[Sencha Touch]]></category>
		<category><![CDATA[Ext4]]></category>
		<guid isPermaLink="false">/?p=646</guid>

					<description><![CDATA[この記事は、 Sencha Advent Calendar 2012 の6日目の記事です。皆さんも是非参加してください。 昨日も書いたのですが、次の人が出てこないので、今日も書きます。 明日はちょっと無理っぽいので、だれ [&#8230;]]]></description>
										<content:encoded><![CDATA[<blockquote>
  <p>この記事は、
  <a href="http://www.adventar.org/calendars/23">Sencha Advent Calendar 2012</a>
  の6日目の記事です。皆さんも是非参加してください。
  昨日も書いたのですが、次の人が出てこないので、今日も書きます。
  明日はちょっと無理っぽいので、だれかお願いします。まぢで。</p>
</blockquote>

<p>今回は、Sencha フレームワークの中のデータパッケージについての話です。
ModelやStoreがサーバーから（いや、クライアントサイドでもですが）データを取得する時には、Proxyを使います。</p>

<p><span id="more-646"></span></p>

<p>このProxy、Ext JS 3 の頃には、Storeにくっつけて使う物でしたが、Ext JS 4やSencha Touch 2では、StoreでもModelでも、どちらにでもくっつけられます。そして、Senchaの公式ドキュメントを見ると、どうもModelにくっつける方が推奨されているようです。</p>

<div>[js]
    Ext.define(&#8216;MyModel&#8217;, {
        extend: &#8216;Ext.data.Model,
        proxy: {
            type: &#8216;ajax&#8217;,
            url: &#8216;data/hoge.php&#8217;,
            reader: {
                type: &#8216;json&#8217;
            }
        },
        fields: [{
            name: &#8216;id&#8217;,
                :
[/js]</div>

<p>このような感じで、Modelにくっつけます。</p>

<p>ProxyをModelにくっつけるのと、Storeにくっつけるのでは、どう違うのでしょうか。
Modelにくっつけるのを推奨している理由は、次の2つが挙げられていました。</p>

<ul>
<li>StoreがなくてもModel単体でサーバーと通信ができる</li>
<li>同じModelを使う複数のStoreがあるときにそれぞれにProxyの設定をする必要が無い</li>
</ul>

<p>なるほど。</p>

<p>そう思って、僕も主にStoreではなくModelにProxyをくっつけて実装していました。
それに僕がProxyだったら、StoreとModelを較べるとModelの方がスレンダーで美人なような感じがしますものね（違</p>

<p>そして、そんなことを言っていると、ある時にハマりまして、そのときにこの件についてもう一度考える機会を得ました。</p>

<blockquote>
  <p>Proxyってほんとはどっちと結婚したらいいの？（だから違</p>
</blockquote>

<h2>ProxyがModelと結婚したら</h2>

<p>ProxyのコンフィグがセットされたModelがインスタンス化されるときの動きを考えます。
Modelというのは、いわばレコードですから、レコードの数だけインスタンスがあることになります。しかしProxyもそれと同じだけインスタンス化されるのは無駄です、100件も1000件ものModelそれぞれにProxyなんていりません。ですから、Proxyのインスタンスは1つしかできません。
ModelにProxyのコンフィグがくっついている場合には、そのModelが使うProxyは1つだけ作られます。そして、そのModelのインスタンスが使われる時には、そのProxyが使われます。</p>

<p><img decoding="async" loading="lazy" src="/wp-content/uploads/2012/12/proxy_fig1.png" alt="" title="proxy_fig1" width="429" height="124" class="alignnone size-full wp-image-648" srcset="/wp-content/uploads/2012/12/proxy_fig1.png 429w, /wp-content/uploads/2012/12/proxy_fig1-300x86.png 300w" sizes="(max-width: 429px) 100vw, 429px" /></p>

<h2>ProxyがStoreと結婚したら</h2>

<p>ではProxyがStoreのコンフィグにくっつけられていたらどうでしょうか。Storeは、Modelのコレクションです。StoreにProxyのコンフィグがある場合は、Storeがインスタンス化されるごとにProxyのインスタンスも作られ、Storeにセットされます。</p>

<p>同じ、Storeクラスからインスタンス化された場合でも、インスタンスが複数作られれば、Proxyも同じ数だけ作られ、Storeにバインドされます。
ModelにもStoreにもProxyが設定されていた場合は、StoreのProxyが優先的に使われます。</p>

<p><img decoding="async" loading="lazy" src="/wp-content/uploads/2012/12/proxy_fig2.png" alt="" title="proxy_fig2" width="429" height="323" class="alignnone size-full wp-image-650" srcset="/wp-content/uploads/2012/12/proxy_fig2.png 429w, /wp-content/uploads/2012/12/proxy_fig2-300x225.png 300w" sizes="(max-width: 429px) 100vw, 429px" /></p>

<h2>まとめます</h2>

<ul>
<li>Modelに設定されたProxyのインスタンスは1つだけ</li>
<li>Storeに設定されたProxyのインスタンスはStoreのインスタンスの数だけ</li>
</ul>

<p>この2つを理解していないとハマる事があります。</p>

<p>僕がはまったのは、extraParamsです。
このコンフィグおよび、setExtraParamはProxyのコンフィグ／メソッドなんですが、
これを使って、サーバーにパラメーターを渡してデータを得ていたのです。
ProxyはModelにセットしていました。</p>

<p>しかも、Storeを複数インスタンス化して、別なパラメーターをsetExtraParamでセットして。</p>

<p>ここで、勘の良い方はお気づきと思いますが、このやり方だとStoreをいくつインスタンス化しても、Proxyは1つの物が共有されるのです。</p>

<p><img decoding="async" loading="lazy" src="/wp-content/uploads/2012/12/proxy_fig3.png" alt="" title="proxy_fig3" width="429" height="273" class="alignnone size-full wp-image-652" srcset="/wp-content/uploads/2012/12/proxy_fig3.png 429w, /wp-content/uploads/2012/12/proxy_fig3-300x190.png 300w" sizes="(max-width: 429px) 100vw, 429px" /></p>

<p>つまりこの図のようになるんですね。だから、setExtraParamでextraParamsの内容を書き換えると、すべてのStoreインスタンスのパラメーターが変わっちまうんです。(´･ω･`)</p>

<p>このような使い方をする場合は、StoreにProxyをくっつけます。そうしたらStoreインスタンスごとにProxyのインスタンスができるので、extraParamsもそれぞれのStoreで保持されます。</p>

<p>以上が、Proxyがどちらを伴侶として選ぶかによって、変わる人生のシミュレーションです（違。</p>

<p>とても深遠な話になりましたね。人生の教訓として覚えておきましょうｗ</p>
]]></content:encoded>
					
					<wfw:commentRss>/646/feed</wfw:commentRss>
			<slash:comments>1</slash:comments>
		
		
			</item>
		<item>
		<title>Ext JSでのOverrideについて</title>
		<link>/632</link>
					<comments>/632#comments</comments>
		
		<dc:creator><![CDATA[sunvisor]]></dc:creator>
		<pubDate>Tue, 04 Dec 2012 19:03:51 +0000</pubDate>
				<category><![CDATA[Ext JS]]></category>
		<category><![CDATA[Sencha Touch]]></category>
		<category><![CDATA[Ext4]]></category>
		<guid isPermaLink="false">/?p=632</guid>

					<description><![CDATA[この記事は、 Sencha Advent Calendar 2012 の5日目の記事です。皆さんも是非参加してください。 明日も明後日も決まってないみたいなんで、誰か書いてくださいよ〜 そこの、青さん！蛙さん！たのみます [&#8230;]]]></description>
										<content:encoded><![CDATA[<blockquote>
  <p>この記事は、
  <a href="http://www.adventar.org/calendars/23">Sencha Advent Calendar 2012</a>
  の5日目の記事です。皆さんも是非参加してください。
  明日も明後日も決まってないみたいなんで、誰か書いてくださいよ〜
  そこの、青さん！蛙さん！たのみますよ。</p>
</blockquote>

<p>通常、クラスを理解するプログラマがオーバーライドという言葉を聞くと、親クラスを継承した中でクラスのメソッドをオーバーライドするというのをイメージすると思います。
通常のクラスシステムでは考えられないことだと思いますが、JavaScript上で実現されているフレームワークであるExt JSのクラスシステムでは、既存のクラスの定義内容をあとから変更することができます。
Ext JSでは、このこともオーバーライドと呼びます。</p>

<p><span id="more-632"></span></p>

<p>通常のオーバーライドでは、クラスを継承してメソッドをオーバーライドしますので、
オーバーライドしたメソッドを呼び出すためには、継承したサブクラスをインスタンス化しなければなりません。
ここで扱うオーバーライドは、元のクラス定義そのものにあるメソッドをオーバーライドするということになります。
その場合、元のクラスをインスタンス化したとしても、オーバーライドしたメソッドが実行されます。</p>

<p>この機能は、Ext JS本体のバグを修正するためのパッチとか、一部特定の場合だけメソッドの挙動を変える必要があるときなどに使われます。
また、Ext JSで提供されているロケールファイルの中身は、このオーバーライドのオンパレードです。
この機能は、以前から、<code>Ext.override</code>メソッドとして提供されていました。
しかし、オーバーライドしたメソッドから、元のメソッドを呼び出すようなことはできませんでした。
Ext JS 4.1からは、元のメソッドを呼び出せるようになったので、パッチ当ての時に元のメソッドと重複したコードを書かずに済む場面が増えることでしょう。
また、同じくExt JS 4.1からは、<code>Ext.override</code>メソッドだけではなく、<code>Ext.define</code>メソッドを使ったオーバーライドもできるようになりました。
ここでは、Ext.defineを使ったオーバーライドについてみていきましょう。</p>

<div>[js]
    // Base Class
    Ext.define(&#8216;My.BaseClass&#8217;, {
        method: function(v) {
            console.log(&#8216;ベースクラスのメソッド&#8217;);
        }
    });
    
    // Class を継承
    Ext.define(&#8216;My.SubClass&#8217;, {
        extend: &#8216;My.BaseClass&#8217;,
        method: function(v) {
            console.log(&#8216;サブクラスのメソッド&#8217;);
        }
    });
[/js]</div>

<p>これは、普通の継承です。
ここで、</p>

<div>[js]
    var c1 = Ext.create(&#8216;My.SubClass&#8217;);
    c1.method();
[/js]</div>

<p>とすれば、コンソールには、「サブクラスのメソッド」と表示されます。
生成するクラスを<code>"My.BaseClass"</code>にした場合は、「ベースクラスのメソッド」とログされます。
ここまでは普通です。</p>

<div>[js]
    // オーバーライド
    Ext.define(&#8216;My.OvrClass&#8217;, {
        override: &#8216;My.BaseClass&#8217;,
        method: function(v) {
            console.log(&#8216;オーバーライドしたメソッド&#8217;);
        }
    });
[/js]</div>

<p>次にこのように、<code>extend</code>指令の代わりに<code>override</code>指令を加えます。
こうすると、クラスを継承するのではなく、クラス定義そのものを上書きするという挙動になります。
第1引数に渡すクラス名は、ここでは意味を持ちません。
つまり、この定義をして、</p>

<div>[js]
    Ext.create(&#8216;My.OvrClass&#8217;);
[/js]</div>

<p>とインスタンス化しようとしてもエラーになります。
なんで、ここでクラス名を指定するかというと、それは<code>Ext.Loader</code>に見つけてもらうため。その為です。
オーバーライドされたクラスを使いたい別のクラスにおいて、<code>required</code>の中にこのクラス名（クラス名じゃないんですが）を入れておくと<code>Ext.Loader</code>様がロードしてくださるわけです。</p>

<p>このようにオーバーライド定義をすると、</p>

<div>[js]
    var c1 = Ext.create(&#8216;My.BaseClass&#8217;);
    c1.method();
[/js]</div>

<p>と、<code>My.BaseClass</code> をインスタンス化した場合に、「オーバーライドしたメソッド」とログされます。
元のメソッドを呼び出すときには、クラス継承したときに親クラスのメソッドを呼び出すときと同様、
<code>callParent</code>を使います。</p>

<div>[js]
    // オーバーライド
    Ext.define(&#8216;My.OvrClass&#8217;, {
        override: &#8216;My.BaseClass&#8217;,
        method: function(v) {
            this.callParent(v);
            console.log(&#8216;オーバーライドしたメソッド&#8217;);
        }
    });
[/js]</div>

<p>尚、このオーバーライドは、サブクラスでも適用されます。BaseClassがオーバーライドされていて、SubClassがBaseClassを継承している場合に、SubClassをインスタンス化してメソッドを呼び出すと、オーバーライドされたメソッドが実行されます。</p>
]]></content:encoded>
					
					<wfw:commentRss>/632/feed</wfw:commentRss>
			<slash:comments>1</slash:comments>
		
		
			</item>
		<item>
		<title>Sencha Cmd がやってきた</title>
		<link>/589</link>
		
		<dc:creator><![CDATA[sunvisor]]></dc:creator>
		<pubDate>Thu, 20 Sep 2012 12:21:03 +0000</pubDate>
				<category><![CDATA[Ext JS]]></category>
		<category><![CDATA[Sencha Touch]]></category>
		<guid isPermaLink="false">/?p=589</guid>

					<description><![CDATA[Sencha Cmd V3 のbetaが本日リリースされました。 以前からあった、Sencha SDK Toolkit (又の名をSencha Command) がリニューアルしたものです。 これは、内部的にはかなりのリ [&#8230;]]]></description>
										<content:encoded><![CDATA[<p>Sencha Cmd V3 のbetaが本日リリースされました。</p>

<p>以前からあった、Sencha SDK Toolkit (又の名をSencha Command) がリニューアルしたものです。
これは、内部的にはかなりのリニューアルがあったようで、その動作環境がJavaになったのだそうです。</p>

<p>まず、Sencha Touch のプロダクトしかサポートしていなかった、アプリケーションのジェネレートが、
Ext JS でもできるようになりました。
また、デプロイ関連では、前のバージョンでは、Phantom JSを使って使われているクラスを判断していたのを、
ソースコードの中を見て判断するようになったようです。
(Sencha Cmdのプロダクトの中を見てみると、まだPhantom.jsが入っているんで、使ってはいるみたいですねぇ)</p>

<p>これまで、結構必要ファイルに漏れがあったので、こんどのものには期待したいところです。
漏れがあったのは、僕自身のrequiresなどの設定が悪かったせいかもしれませんが、
いろいろ試したけどだめだったんすよねぇ〜</p>

<p>とまれ、Sencha 社内でも大評判だというこのツールに期待大です。
<span id="more-589"></span></p>

<p>なお、最新の機能は Ext JS に対応していますが、
Sencha Touch には未対応なようです。
後のバージョンではサポートするよというようなことが書いてありました。</p>

<p>そうそう、この情報の元ネタのブログは
<a href="http://www.sencha.com/blog/all-new-sencha-cmd/">こちら</a>、
そして、速攻翻訳が
<a href="http://www.xenophy.com/javascript/3653">こちら</a>
になります。
ダウンロード先も書いてあります。</p>

<p>今回はドキュメントも充実しています。
Ext JS 4.1 のAPIドキュメントのガイドに沢山の記事があります。</p>

<p><a href="http://docs.sencha.com/ext-js/4-1/#!/guide/command">http://docs.sencha.com/ext-js/4-1/#!/guide/command</a></p>

<p>英文なので、僕も全部は読めていませんが、これを一通り読めば良い感じに使えるようになるんじゃないでしょうか。</p>

<h2>必要条件</h2>

<ol>
<li>Java Run-time</li>
<li>Compass</li>
<li>Sencha Cmd</li>
<li>Ext JS は 4.1.1a または 4.1.2a が必要。無印の4.1.1や4.1.2はだめ。</li>
</ol>

<p>僕は、当日早速落としたSencha Cmdで試していて、
全然うまく動かずに泣いていたのですが、
その日のうちに、バグフィックスのアップデートがされていて、
それを適用したら直りました。</p>

<p>また、Sencha SDK Toolkit をインストール済みの場合は、
コマンド名が同じSenchaのため、.profile などを編集して、
SDK Toolkit が設定したPATHをコメントアウトするなどの対策が必要です。</p>

<h2>つかってみるべ</h2>

<p>インストール後に、</p>

<pre><code>$ sencha
</code></pre>

<p>で、</p>

<pre><code>Sencha Cmd v3.0.0.141

OPTIONS
  * --debug, -d - Sets log level to higher verbosity
  * --plain, -p - enables plain logging output (no highlighting)
  * --quiet, -q - Sets log level to warnings and errors only
  * --sdk-path, -s - sets the path to the target framework

CATEGORIES
  * compile - Compile sources to produce concatenated output and metadata
  * generate - Generate code like models and controllers or raw templates
  * theme - Builds a set of theme images from a given html page
  * app - Perform various application build processes
  * fs - A set of useful utility actions to work with files.
  * package - Packages a Sencha Touch application for native app stores
  * manifest - Extract class metadata

COMMANDS
  * ant - Invoke Ant with helpful properties back to Sencha Command
  * build - Builds a project from a JSB3 file.
  * config - Loads a config file or sets a configuration property
  * help - Get help on using Sencha Command
</code></pre>

<p>こんな感じで表示されたらOKです。</p>

<p>で、最初にプロダクトを作ってみます。</p>

<pre><code>$ sencha -sdk ~/Downloads/ExtJS/ext-4.1.1a generate app MyApp ~/devsites/cmd_test/public_html
</code></pre>

<p>と実行してみました。</p>

<ul>
<li>-sdk の後はExt JSのディレクトリ。sdkのディレクトリをカレントディレクトリとして実行したら省略可能。</li>
<li>generate で、アプリケーションのスカッフォルドを生成。</li>
<li>app の次にアプリケーションのネームスペースと、生成するディレクトリを指定。</li>
</ul>

<p>コンソールに [INFO] &#8230;. と表示され、完了したら、
そのディレクトリにアプリケーションの枠組みができあがりました。</p>

<pre><code>  ▾ public_html/
    ▾ app/
      ▾ controller/
          Main.js
      ▾ view/
          Main.js
          Viewport.js
        app.js
    ▸ ext/
    ▸ resources/
      app.json
      bootstrap.js
      index.html
</code></pre>

<p>index.html の中身は次のとおり</p>

<pre><code>&lt;!DOCTYPE HTML&gt;
&lt;html&gt;
&lt;head&gt;
    &lt;meta charset="UTF-8"&gt;
    &lt;title&gt;MyApp&lt;/title&gt;
    &lt;link rel="stylesheet" href="resources/css/default/app.css"&gt;
    &lt;!-- &lt;x-compile&gt; --&gt;
        &lt;!-- &lt;x-bootstrap&gt; --&gt;
            &lt;script src="ext/ext-debug.js"&gt;&lt;/script&gt;
            &lt;script src="bootstrap.js"&gt;&lt;/script&gt;
        &lt;!-- &lt;/x-bootstrap&gt; --&gt;
        &lt;script src="app/app.js"&gt;&lt;/script&gt;
    &lt;!-- &lt;/x-compile&gt; --&gt;
&lt;/head&gt;
&lt;body&gt;&lt;/body&gt;
&lt;/html&gt;
</code></pre>

<p>bootstrap.js の中身は</p>

<pre><code>Ext.Loader.addClassPathMappings({
  "Ext.draw.engine.ImageExporter": "ext/src/draw/engine/ImageExporter.js",
  "Ext.layout.component.Auto": "ext/src/layout/component/Auto.js",
  "Ext.grid.property.Store": "ext/src/grid/property/Store.js",
  "Ext.layout.container.Box": "ext/src/layout/container/Box.js",
  "Ext.direct.JsonProvider": "ext/src/direct/JsonProvider.js",
  :
  :
</code></pre>

<p>な感じです。</p>

<p>app.json はコメントが沢山入っていますが、はずすと次の内容です。</p>

<pre><code>{
    "name": "MyApp",

    "js": [
        {
            "path": "ext/ext-all.js"
        },
        {
            "path": "app/app.js",
            "update": "delta"
        }
    ],

    "css": [
        {
            "path": "resources/css/default/app.css",
            "update": "delta"
        }
    ],

    "appCache": {
        "cache": [
            "index.html"
        ],
        "network": [
            "*"
        ],
        "fallback": []
    },

    "extras": [
        "resources/images",
        "resources/icons",
        "resources/loading"
    ],

    "archivePath": "archive",

    "buildPaths": {
        "testing": "build/testing",
        "production": "build/production",
        "package": "build/package",
        "native": "build/native"
    },

    "buildOptions": {
        "product": "touch",
        "minVersion": 3,
        "debug": false,
        "logger": "no"
    },

    "id": "8b895996-e12d-44d0-ad5d-579508c94e6d"
}
</code></pre>

<p>僕としては、最も気になるのは、デプロイの時にちゃんと使えるのかな？
というところです。
ブログの文章では、最終的にデプロイをSencha Cmdでやりたい場合は、
開始からSencha Cmdでジェネレートした方がいいような感じもしましたが、
既存のプロジェクトのデプロイも含めて、いろいろ実験したいと思います。</p>
]]></content:encoded>
					
		
		
			</item>
		<item>
		<title>Sencha Touch 2 日本語ロケールファイル</title>
		<link>/581</link>
		
		<dc:creator><![CDATA[sunvisor]]></dc:creator>
		<pubDate>Sat, 15 Sep 2012 02:09:42 +0000</pubDate>
				<category><![CDATA[Sencha Touch]]></category>
		<guid isPermaLink="false">/?p=581</guid>

					<description><![CDATA[Sencha Touch 2 の日本語ロケールファイルってないんですよね。 Ext JS 4 にはあるんですがね。 ないなら、作ってみようと思い立ち作ってみました。 ファイル名を ext-lang-ja.js として、i [&#8230;]]]></description>
										<content:encoded><![CDATA[<p>Sencha Touch 2 の日本語ロケールファイルってないんですよね。</p>

<p>Ext JS 4 にはあるんですがね。</p>

<p>ないなら、作ってみようと思い立ち作ってみました。
ファイル名を ext-lang-ja.js として、index.html で読み込んでください。
MessageBox とかが日本語化されます。
まだ、試作品です。テストも完了していません。
いろいろなフィードバックもらえるととてもうれしいです。</p>

<p>今の、バージョンの Ext JS や Sencha Touch は Ext.define の override を使って
ロケールを適用しているのですね。Ext.define({override:&#8230;}); すごいわ。</p>

<ul>
<li>ソースコードは、github.com の <a href="https://gist.github.com/3726024">gist</a> に入っています。</li>
<li>また、ご意見などはここのコメントでもいいですが、
<a href="http://www.meetup.com/Japan-Sencha-UserGroup/messages/boards/thread/26943842">Sencha UGのDiscussions</a>
の方にしていただけるとうれしいです。</li>
</ul>

<p><span id="more-581"></span></p>

<script src="https://gist.github.com/3726024.js"> </script>
]]></content:encoded>
					
		
		
			</item>
		<item>
		<title>Sencha Touch 2を使用したアプリケーション入門</title>
		<link>/525</link>
		
		<dc:creator><![CDATA[sunvisor]]></dc:creator>
		<pubDate>Sat, 03 Mar 2012 06:17:12 +0000</pubDate>
				<category><![CDATA[Sencha Touch]]></category>
		<guid isPermaLink="false">/?p=525</guid>

					<description><![CDATA[Sencha Touch 2.0 のRCが公開されています。 そのドキュメントのガイドにある Intro to Applications with Sencha Touch 2 を翻訳しました。 Sencha Touch [&#8230;]]]></description>
										<content:encoded><![CDATA[<p>Sencha Touch 2.0 のRCが公開されています。
そのドキュメントのガイドにある
Intro to Applications with Sencha Touch 2
を翻訳しました。
<span id="more-525"></span></p>

<h1>Sencha Touch 2を使用したアプリケーション入門</h1>

<p>Sencha Touch 2は、複数のプラットフォームで動作するアプリケーションを構築することに最適化されています。
できる限り簡単にアプリケーションを構築するために、
シンプルですがパワフルなMVC(モデル、ビュー、コントローラー)パターンを実現するアプリケーションアーキテクチャーを提供します。
このアプローチはコードをクリーンでテスト可能で保守しやすく保ち、アプリケーションを書く上でのいくつかの恩恵を提供します。</p>

<ul>
<li><strong>履歴のサポート</strong>: アプリケーション内で戻るボタンをサポートし、アプリケーションのどのバートにもリンクできるようにします。</li>
<li><strong>ディープ リンク</strong>: ウェブページへのリンクのようにアプリケーションのスクリーンを開くディープリンクを提供します。</li>
<li><strong>デバイス プロファイル</strong>: 携帯電話、タブレット、他のデバイスによって簡単にアプリケーションのUIをカスタマイズでき、
共通のコードを共有できます。</li>
</ul>

<h2>アプリケーションの構造</h2>

<p>アプリケーションは
Model, View, Controller, Store, Profile 
さらにアプリケーションアイコンや起動画面の画像などを指定する
いくつかのメタデータの集合体です。</p>

<p><img decoding="async" src="http://docs.sencha.com/touch/2-0/guides/apps_intro/architecture-overview.png" alt="architecture-overview" /></p>

<ul>
<li><strong>Model</strong>:
アプリケーションのオブジェクトのタイプを表現します。例えばeコマースのアプリであればUsers, Products, Orders などのモデルを持つことになります。</li>
<li><strong>View</strong>:
ユーザーにデータを表示しSencha Touchのビルトインコンポーネントを活用します。</li>
<li><strong>Controller</strong>:
アプリケーションとのやりとりをハンドリングし、ユーザのタップやスワイプをリスニングし、それに応じたアクションを起こします。</li>
<li><strong>Store</strong>:
アプリにデータをロードしてListやDataViewなどのコンポーネントに供給します。</li>
<li><strong>Profile</strong>:
多くのコードを共有したままアプリケーションのUIをタブレットや携帯電話ごとに簡単にカスタマイズできるようにします。</li>
</ul>

<p>アプリケーションはまずSencha Touchアプリケーションを定義することから始めるのが普通です。次のようになります。</p>

<div>[js]
    Ext.application({
        name: &#8216;MyApp&#8217;,
        models: [&#8216;User&#8217;, &#8216;Product&#8217;, &#8216;nested.Order&#8217;],
        views: [&#8216;OrderList&#8217;, &#8216;OrderDetail&#8217;, &#8216;Main&#8217;],
        controllers: [&#8216;Orders&#8217;],

        launch: function() {
            Ext.create(&#8216;MyApp.view.Main&#8217;);
        }
    });
[/js]</div>

<p>nameコンフィグはアプリケーション全体で使う1つのグローバルネームスペースを生成するために使います。
すべてのモデル、ビュー、コントローラーなどのクラスはこのネームスペースに含まれます。
<em>MyApp</em>というアプリでは、それを構成するクラスは
<em>MyApp.model.User</em>, <em>MyApp.controller.Users</em>, <em>MyApp.view.Main</em>
などのようなパターンになります。
これによりアプリ全体が1つのグローバル変数の下に保持されますので、ページ上の他のコードと衝突を起こす可能性が最小になります。</p>

<p>アプリケーションに <em>models</em>, <em>views</em>, <em>controllers</em> コンフィグを定義すると、これらのクラスをアプリに自動的にロードします。
簡単なファイル構造規則
(モデルはapp/modelディレクトリに、コントローラーはapp/controllerディレクトリに、ビューはapp/viewディレクトリに)
に従います。
例) <em>app/model/User.js</em>, <em>app/controllers/Orders.js</em>, <em>app/view/Main.js</em></p>

<p>modelsに指定したうちの最後の1つが他と違ってますね。
これはフルクラス名でいうと
(&#8220;MyApp.model.nested.Order&#8221;)
になります。
標準の名付け規則に従わない場合には、これらのコンフィグではフルクラス名を指定することができます。
カスタムの依存関係を指定する方法については
<a href="http://docs.sencha.com/touch/2-0/#!/api/Ext.app.Application">Ext.app.ApplicationのドキュメントにあるDependenciesのセクション</a>
を参照してください。</p>

<h2>コントローラー</h2>

<p>コントローラーはアプリケーションをまとめる接着剤です。</p>

<p>UIで発火されたイベントをリッスンしてそのアクションを実行します。
これによりコードが綺麗になり読みやすくなりコントロールロジックからビューを隔離することができます。</p>

<p>例えば、ログインフォームを使ってログインする必要があるとしましょう。
この場合のビューは入力フィールドや他のコントロールのあるフォームです。
コントローラーはフォームの送信ボタンのtapイベントをリッスンして、認証作業を実行します。
データや状態の操作があったときには、ビューではなくコントローラーがその変更を処理するべきです。</p>

<p>コントローラは小さなしかしパワフルな機能のセットを公開します、
そしてちょっとした単純な規則に従います。</p>

<p>アプリケーション内の各コントローラーはExt.app.Controllerのサブクラスです
(既存のコントローラーのサブクラスでもいいのですが、それもExt.app.Controllerを継承していることに違いはありませんね)</p>

<p>コントローラーはMyApp.controller.*ネームスペースに存在します。
例えばSessionsコントローラーがあるとすると、その名前は
MyApp.controller.Sessions
となり、ファイル名は
app/controller/Sessions.js.
となります。</p>

<p>コントローラーはExt.app.Controllerのサブクラスですが、それぞれはExt.app.Applicationによってロードされるときに一度だけインスタンス化されます。</p>

<p>同時に1つしかインスタンスは存在せずコントローラーのインスタンスのセットはApplicationの内部で管理されます。
Applicationのcontrollersコンフィグを使うと、コントローラーをロードし自動的にインスタンス化します。</p>

<h3>シンプルな例</h3>

<p>上記で説明したSessionsコントローラーを定義します。
ここでは2つのコンフィグを指定しています。refs と control です。
refsはページ上のコンポーネントを見つけ出す簡単な方法です。
この場合ですとxtypeがformpanelである全てのコンポーネントにマッチし、
見つかった中の最初のものが<em>loginForm</em>プロパティに割り当てられます。</p>

<p>このプロパティは後でdoLogin関数内で使います。</p>

<p>次はcontrolコンフィグです。
これもrefsのようにComponentQueryセレクターを使ってxtypeが<em>formpanel</em>の中にある<em>button</em>を探します。
(この例では、ログインフォームにあるSubmitボタンが見つかります)
このタイプのボタンがtapイベントを発火した場合は、<em>doLogin</em>関数がコールされます。</p>

<div>[js]
    Ext.define(&#8216;MyApp.controller.Sessions&#8217;, {
        extend: &#8216;Ext.app.Controller&#8217;,

        config: {
            refs: {
                loginForm: &#8216;formpanel&#8217;
            },
            control: {
                &#8216;formpanel button&#8217;: {
                    tap: &#8216;doLogin&#8217;
                }
            }
        },

        doLogin: function() {
            var form   = this.getLoginForm(),
                values = form.getValues();

            MyApp.authenticate(values);
        }
    });
[/js]</div>

<p>doLogin関数の本体は非常に簡単です。
ref に　&#8217;loginForm&#8217; を定義したので、コントローラーは一致する<em>formpanel</em>を返す<em>getLoginForm</em>関数を自動的に生成します。
このフォームへの参照から値(usernameとpassword)を引っ張り出して、<em>authenticate</em>関数に渡します。
これら &#8211;
イベントの発火(大概はUIからの)をリッスンしてアクション(この場合は認証)をキックする &#8211;
がほとんどのコントローラーがやることです。</p>

<p>コントローラとは何でどんな機能があるのかについて詳しくは
<a href="http://docs.sencha.com/touch/2-0/#!/guide/controllers">コントローラーのガイド</a>
を参照してください。</p>

<h2>ストア</h2>

<p>ストアはSenca Touch の重要なパートでデータ連係ウィジェットのほとんどにデータを供給します。
簡単に言うと、ストアはModelインスタンスの配列以上のものではありません。
ListやDataViewのようなデータ連係コンポーネントはストアの中の各Modelインスタンスの1つのアイテムだけを描画します。
Modelインスタンスがストアに追加されたり削除されたりするとイベントが発火し、それをデータ連係コンポーネントがリッスンして自分自身を更新します。</p>

<p><a href="http://docs.sencha.com/touch/2-0/#!/guide/stores">ストアのガイド</a>
には
ストアが何でありアプリのコンポーネントとどのようにフィットさせるのかについて
より多くの情報があります。
そこには、Applicationインスタンスと統合する意識しておくべきいくつかの具体的なポイントがあります。</p>

<h2>デバイス プロファイル</h2>

<p>Sencha Touchは、能力やスクリーンサイズが異なる幅広い範囲のデバイスで使われます。
タブレットで動作するユーザーインターフェースは携帯電話ではうまく動かないかもしれませんし、その逆もまた然りですので、違ったデバイスタイプ用にカスタマイズされたビューを提供するのは当然です。</p>

<p>しかしながら、違ったUIを提供するだけのために何度もアプリケーションを書きたくはありません。
できるだけ多くのコードを共有したいと考えます。</p>

<p>デバイス プロファイルはアプリがサポートする違ったタイプのデバイスを定義することができるシンプルなクラスで、
各デバイスは違った方法で処理されます。
これらはオプトインなので、最初はプロファイルなしで開発してあとで追加することも、プロファイルを使わないこともできます。
各プロファイルには、
プロファイルがアクティブになるべき時にはtrueを返すisActive関数や、
プロファイルが決定されたときにロードする
<em>models</em>
<em>views</em>
<em>controllers</em>
コンフィグを定義します。</p>

<p>アプリがプロファイルをサポートするようにするには、
これらのプロファイルをApplicationのprofilesコンフィグに設定すると、
ApplicationはExt.app.Profileのサブクラスを生成します。</p>

<div>[js]
    Ext.application({
        name: &#8216;MyApp&#8217;,
        profiles: [&#8216;Phone&#8217;, &#8216;Tablet&#8217;],

        //as before
    });
[/js]</div>

<p>上記のように設定するとApplicationはapp/profile/Phone.js と app/profile/Tablet.jsをロードします。
アプリのタブレットバージョンでは追加機能が利用可能になる
グループ管理の例を見てみましょう。
次がTabletプロファイルの定義例です。</p>

<div>[js]
    Ext.define(&#8216;MyApp.profile.Tablet&#8217;, {
        extend: &#8216;Ext.app.Profile&#8217;,

        config: {
            controllers: [&#8216;Groups&#8217;],
            views: [&#8216;GroupAdmin&#8217;],
            models: [&#8216;MyApp.model.Group&#8217;]
        },

        isActive: function() {
            return Ext.os.is.Tablet;
        }
    });
[/js]</div>

<p>isActive関数はアプリケーションがSencha Touchがタブレットであると判断したもので動作している場合にtrueを返します。
これは少々主観的な判断になります。というのもデバイスのシェイプやサイズはほぼ連続的に分布していて、携帯電話とタブレットの間の明確な分岐点はないからです。
どのデバイスが携帯電話でどれがタブレットなのかという判断をする確実な方法がないので、
Sencha Touchの <em>Ext.os.is.Table</em>は、iPadで実行しているときにはtrueそれ以外はfalseを返します。
より細かなコントロールをしたい場合には、<em>isActive</em>関数で好きなように実装を提供してtureまたはfalseを返すようにします。</p>

<p>isActive関数がtrueを返すプロファイルは1つだけにしなければなりません。
複数のプロファイルがtrueを返す場合は、最初の1つだけがカウントされ他は無視されます。
trueを返す最初の1つがApplicationのcurrentProfileにセットされ、それはあとから参照できます。</p>

<p>currentProfileに models,, views, controllers, sotres コンフィグが定義されていたら、
Applicationは、自身で定義された全ての<em>models, *views</em>, <em>controllers</em>とともにそれらを自動的にロードします。
しかし、プロファイルの中で名付けられた依存関係は、
完全修飾形式のクラス名が指定されない限りプロファイル名が前につけられたものになります。</p>

<ul>
<li><em>views: [&#8216;GroupAdmin&#8217;]</em> では <em>app/view/tablet/GroupAdmin.js</em> がロードされます。</li>
<li><em>controllers: [&#8216;Groups&#8217;]</em> では <em>app/controller/tablet/Groups.js</em> がロードされます。</li>
<li><em>models: [&#8216;MyApp.model.Group&#8217;]</em> では <em>app/model/Group.js</em> がロードされます。</li>
</ul>

<p>ほとんどの場合は、プロファイルは追加のコントローラーやビューを追加するだけで、モデルやストアは全てのタイプのアプリの間で共有されるのが普通です。</p>

<p>プロファイルについてのより詳しい説明は
<a href="http://docs.sencha.com/touch/2-0/#!/guide/profiles">device profiles guide</a>
を参照してください。</p>

<h2>起動プロセス</h2>

<p>各Applicationではlaunchメソッドを定義できます。
これはアプリのクラスがロードされアプリを起動する準備ができたらコールされます。
This is usually the best place to put any application startup logic, typically creating the main view structure for your app.
これは、アプリケーションのスタートアップロジック
(よくあるのがアプリのメインビュー構造を生成するなど)
を書く最良の場所です。</p>

<p>Applicationのlaunch関数の他に、スタートアップロジックを配置する場所があと2つあります。
まず各コントローラーにはinit関数を定義することができます。この関数はApplicationのlaunch関数の前に呼び出されます。
次にデバイスプロファイルを使っている場合には各プロファイルにlaunch関数を定義できます。
これはコントローラーのinit関数の後、Applicationのlaunch関数の前にコールされます。</p>

<p>注意すべきはアクティブなプロファイルのlaunch関数だけが呼び出されるということです。
例えば、PhoneとTabletのプロファイルが定義されていてタブレットで実行された場合には、
Tabletプロファイルのlaunch関数だけがコールされます。</p>

<ol>
<li>Controller#init 関数が呼ばれる</li>
<li>Profile#launch 関数が呼ばれる</li>
<li>Application#launch 関数が呼ばれる</li>
<li>Controller#launch 関数が呼ばれる</li>
</ol>

<p>プロファイルを使う場合にはプロファイルのlaunch関数の中に起動ロジックのほとんどを配置するのが普通です。
なぜなら各プロファイル毎に開始時に組み立てられる必要があるビューのセットが違うからです。</p>

<h2>ルーティングと履歴のサポート</h2>

<p>Sencha Touch 2 はルーティングと履歴を完全にサポートします。
Kitchen SinkなどのいくつかのSDKサンプルは、スクリーン間でのナビゲートを簡単にする戻るボタン(特にAndroidで便利)をサポートするために履歴サポートを使っています。</p>

<p>履歴サポートのドキュメントはbeta1以降に公開されます。(訳注: すでに公開済み)
2.0.0 PR4時点でのSencha Touch の履歴サポートを学ぶ最適な場所はKitchin Sinkサンプルです。
これは履歴サポートのために必要なルーティングと状態回復の沢山のドキュメントを備えています。</p>

<!-- Dispatch/redirect
Router
Restoring State -->

<h2>参考文献</h2>

<p>Sencha Touch 2 でのアプリケーション アーキテクチャーを使うことに関するガイドがいくつかあります。</p>

<ul>
<li><a href="http://docs.sencha.com/touch/2-0/#!/guide/controllers">Controllers</a></li>
<li><a href="http://docs.sencha.com/touch/2-0/#!/guide/profiles">Device Profiles</a></li>
<li><a href="http://docs.sencha.com/touch/2-0/#!/guide/history_support">History Support</a></li>
<li><a href="http://docs.sencha.com/touch/2-0/#!/guide/views">Views</a>
<!-- * <a href="http://docs.sencha.com/touch/2-0/#!/guide/testing_mvc">Testing Applications</a> --></li>
</ul>
]]></content:encoded>
					
		
		
			</item>
		<item>
		<title>Building your First App</title>
		<link>/518</link>
		
		<dc:creator><![CDATA[sunvisor]]></dc:creator>
		<pubDate>Fri, 02 Mar 2012 06:36:15 +0000</pubDate>
				<category><![CDATA[Sencha Touch]]></category>
		<guid isPermaLink="false">/?p=518</guid>

					<description><![CDATA[Sencha Touch 2.0 のRCが公開されています。 そのドキュメントのガイドにある Building your First App を翻訳しました。 最初のアプリを作る 準備 このガイドは Getting St [&#8230;]]]></description>
										<content:encoded><![CDATA[<p>Sencha Touch 2.0 のRCが公開されています。
そのドキュメントのガイドにある
Building your First App
を翻訳しました。
<span id="more-518"></span></p>

<h1>最初のアプリを作る</h1>

<h2>準備</h2>

<p>このガイドは
<a href="http://docs.sencha.com/touch/2-0/#!/guide/getting_started">Getting Started Guide</a>
(翻訳は<a href="514">こちら</a>)
で設定されたSDKがセットアップされ環境が完全に機能している前提に立脚しています。</p>

<p>まだでしたら、まず先のガイドを読んでから(数分しかかかりません)ここに戻ってきて、
最初のアプリケーションを作成することを勧めます。</p>

<h2>何を作るか</h2>

<p>あなたの会社のモバイルサイトに使えるような、
簡単なWebサイトのようなモバイルアプリケーションを作成しようと思います。
ホームページ、コンタクトフォーム、最新のブログポストを取得するシンプルなリストを追加し、
それらを携帯電話で読めるようにします。</p>

<p>This is what we&#8217;re going to build (it&#8217;s interactive, try it yourself):
これが作ろうとするものです(インタラクティブです実際に試してください)</p>

<p><a href="http://docs.sencha.com/touch/2-0/#!/guide/first_app">本家のガイドへ行って見てみましょう</a></p>

<h2>はじめに</h2>

<p>最初にするのは
<a href="#!/guide/getting_started">Getting Started Guide</a>
でやったようなアプリケーションのセットアップです。
アプリケーションでは4つのページを持つタブパネル(Ext.tab.Panel)を使います。
最初は次のようにします。</p>

<div>[js]
    Ext.application({
        name: &#8216;Sencha&#8217;,

        launch: function() {
            Ext.create(&quot;Ext.tab.Panel&quot;, {
                fullscreen: true,
                items: [
                    {
                        title: &#8216;Home&#8217;,
                        iconCls: &#8216;home&#8217;,
                        html: &#8216;Welcome&#8217;
                    }
                ]
            });
        }
    });
[/js]</div>

<p>これをブラウザーで実行すると(本家のガイドではLive Previewボタンで実行できるようになっています)、
TabPanelがスクリーンの上部に表示されます。
ホームページはもうちょっといらっしゃいな幹事にしたいので、内容を追加してタブバーをページの下部に変更しましょう。
tabBarPositionコンフィグを追加して、htmlコンフィグにコンテンツを追加します。</p>

<div>[js]
    Ext.application({
        name: &#8216;Sencha&#8217;,

        launch: function() {
            Ext.create(&quot;Ext.tab.Panel&quot;, {
                fullscreen: true,
                tabBarPosition: &#8216;bottom&#8217;,

                items: [
                    {
                        title: &#8216;Home&#8217;,
                        iconCls: &#8216;home&#8217;,
                        html: [
                            &#8216;&lt;img src=&quot;http://staging.sencha.com/img/sencha.png&quot; /&gt;&#8217;,
                            &#8216;&lt;h1&gt;Welcome to Sencha Touch&lt;/h1&gt;&#8217;,
                            &quot;&lt;p&gt;You&#8217;re creating the Getting Started app. This demonstrates how &quot;,
                            &quot;to use tabs, lists and forms to create a simple app&lt;/p&gt;&quot;,
                            &#8216;&lt;h2&gt;Sencha Touch (2.0.0)&lt;/h2&gt;&#8217;
                        ].join(&quot;&quot;)
                    }
                ]
            });
        }
    });
[/js]</div>

<p>Previewボタンをクリックして見てみると、HTMLのコンテンツが表示されますが、あんまりかっこよくありません。
clsコンフィグを追加して、CSSクラスを追加して見栄えを良くします。
ここで追加したCSSは、ダウンロードしたSDKのexmaples/getting_started/index.htmlファイルの中にあります。
それでは、ホームページを見てみます。</p>

<p>(実際の画面は本家でお確かめください）</p>

<h2>ブログページを追加する</h2>

<p>これでちゃんとした見た目のホームページができました、次の画面に移りましょう。
各ページのコードを簡単に追えるように、
一度に一つの退部を作成することにし、、最後にすべてをまとめることにします。</p>

<p>最初のタブを削除してかわりにListを配置します。
フィードをフェッチするのにはGoogleの
<a href="https://developers.google.com/feed/v1/reference">Feed API service</a>
を使うことにします。
ここにもう少しの関係するコードがありますが、まず実行結果を見てみましょう。
そうすればどのように完成したか見ることができます。</p>

<p>(例によって実行結果は本家を参照してください)</p>

<p>Code Editorボタンをクリックすると全てのコードを見ることができますが、部分部分にわけで見ていきましょう。
パネルの代わりにExt.dataview.NestedListをを今回は使います、
そしてsencha.com/blogから最新のブログポストをフェッチします。
NestedListを使って、ただリストをタップするだけでブログエントリーの中にドリルダウンできます。</p>

<p>上記のコードをリストだけにしぼって見てみましょう。</p>

<div>[js]
    Ext.application({
        name: &#8216;Sencha&#8217;,

        launch: function() {
            Ext.create(&quot;Ext.tab.Panel&quot;, {
                fullscreen: true,
                tabBarPosition: &#8216;bottom&#8217;,

                items: [
                    {
                        xtype: &#8216;nestedlist&#8217;,
                        title: &#8216;Blog&#8217;,
                        iconCls: &#8216;star&#8217;,
                        displayField: &#8216;title&#8217;,

                        store: {
                            type: &#8216;tree&#8217;,

                            fields: [
                                &#8216;title&#8217;, &#8216;link&#8217;, &#8216;author&#8217;, &#8216;contentSnippet&#8217;, &#8216;content&#8217;,
                                {name: &#8216;leaf&#8217;, defaultValue: true}
                            ],

                            root: {
                                leaf: false
                            },

                            proxy: {
                                type: &#8216;jsonp&#8217;,
                                url: &#8216;https://ajax.googleapis.com/ajax/services/feed/load?v=1.0&amp;q=http://feeds.feedburner.com/SenchaBlog&#8217;,
                                reader: {
                                    type: &#8216;json&#8217;,
                                    rootProperty: &#8216;responseData.feed.entries&#8217;
                                }
                            }
                        }
                    }
                ]
            });
        }
    });
[/js]</div>

<p>NestedListに少しのコンフィグレーションを与えています &#8211; title, iconCls, displayField &#8211;
そしてちょっと複雑なのがstore。
storeコンフィグはNestedListにデータをフェッチする方法を指示します。
次にstoreの各コンフィグを見てみましょう。</p>

<ul>
<li><em>type: tree</em> NestedListが使うExt.data.TreeStoreを生成します。</li>
<li><em>fields</em> ストアにブログデータの中から取り出したいフィールド(title, content, author など)を指定します。</li>
<li><em>proxy</em> ストアにどこからデータをフェッチするのかを指示します。これつにいては後で詳述します。</li>
<li><em>root</em> ルートノードはleafではないと指定しています。
前のコードでdefaultValueをtrueと指定しているので、ここでそれを上書きする必要があります。</li>
</ul>

<p>全てのStoreコンフィグによりプロキシーはほぼ動作します。
プロキシーにGoogleの
<a href="https://developers.google.com/feed/v1/reference">Feed API service</a>
を使い、ブログデータをJSON-Pフォーマットで返すように指示しています。</p>

<p>これによりあらゆるブログのフィードデータを簡単に取得でき、それをこのアプリケーションに表示できます。
(例えばSenchaブログのURLを
http://rss.slashdot.org/Slashdot/slashdot
に変えて、Slashdotのフィードを見るようにできます)</p>

<p>プロキシー定義の最後の部分はReaderです。
ReaderはGoogleからのレスポンスデータを使えるデータにデコードします。
Googleがブログデータを返すときには、次のようなJSONオブジェクトの中に入って返されます。</p>

<div>[js]
    {
        responseData: {
            feed: {
                entries: [
                    {author: &#8216;Bob&#8217;, title: &#8216;Great Post&#8217;, content: &#8216;Really good content&#8230;&#8217;}
                ]
            }
        }
    }
[/js]</div>

<p>必要なのはentries配列ですから、ReaderのrootPropertyに&#8217;responseData.feed.entries&#8217;と設定しあとはフレームワークにやらせます。</p>

<h3>掘る</h3>

<p>これでNestedListのフェッチと表示ができました。最後にするべきなのはユーザーがエントリーを開いて読めるようにすることです。
あと2つのコンフィグをNestedListに追加して終わりです。</p>

<div>[js]
    {
        xtype: &#8216;nestedlist&#8217;,
        //all other configurations as above
        
        detailCard: {
            xtype: &#8216;panel&#8217;,
            scrollable: true,
            styleHtmlContent: true
        },

        listeners: {
            itemtap: function(nestedList, list, index, element, post) {
                this.getDetailCard().setHtml(post.get(&#8216;content&#8217;));
            }
        }
    }
[/js]</div>

<p>ここでは、detailCardを設定しています。detailCardはNestedListの便利な機能で、
ユーザーがアイテムをタップしたときに違うビューに表示させることができます。
上記で設定したdetailCardは、スクローラブルなパネルで、styleHtmlContentを使ってテキストの見栄えを良くできます。</p>

<p>パズルの最後のピースはitemtapリスナーを追加することです。これはアイテムがタップされた時に起動する関数です。
この関数がやっているのはdetailCardのhtmlにタップされたポストの内容をセットしているだけです。
フレームワークが残りを処理して、ポストの内容が表示される詳細カードにアニメートさせます。
ブログリーダーが動作するために書いたコードはこの1行だけです。</p>

<h2>コンタクトフォームを作る</h2>

<p>最後にこのアプリケーションにコンタクトフォームを作りましょう。
ユーザー名、E-mailアドレス、メッセージを取得するようにします。またExt.form.FieldSetを使って見栄えを良くします。
この機能のコードは簡単です。</p>

<div>[js]
    Ext.application({
        name: &#8216;Sencha&#8217;,

        launch: function() {
            Ext.create(&quot;Ext.tab.Panel&quot;, {
                fullscreen: true,
                tabBarPosition: &#8216;bottom&#8217;,

                items: [
                    {
                        title: &#8216;Contact&#8217;,
                        iconCls: &#8216;user&#8217;,
                        xtype: &#8216;formpanel&#8217;,
                        url: &#8216;contact.php&#8217;,
                        layout: &#8216;vbox&#8217;,

                        items: [
                            {
                                xtype: &#8216;fieldset&#8217;,
                                title: &#8216;Contact Us&#8217;,
                                instructions: &#8216;(email address is optional)&#8217;,
                                items: [
                                    {
                                        xtype: &#8216;textfield&#8217;,
                                        label: &#8216;Name&#8217;
                                    },
                                    {
                                        xtype: &#8216;emailfield&#8217;,
                                        label: &#8216;Email&#8217;
                                    },
                                    {
                                        xtype: &#8216;textareafield&#8217;,
                                        label: &#8216;Message&#8217;
                                    }
                                ]
                            },
                            {
                                xtype: &#8216;button&#8217;,
                                text: &#8216;Send&#8217;,
                                ui: &#8216;confirm&#8217;,
                                handler: function() {
                                    this.up(&#8216;formpanel&#8217;).submit();
                                }
                            }
                        ]
                    }
                ]
            });
        }
    });
[/js]</div>

<p>今回は、Ext.form.FieldSetを配置したExt.form.Panelを生成しているだけです。
フィールドセットには3つのフィールドがあり、1つはname、1つはemail、そしてもう1つはmessageです。
VBoxレイアウトを使って、アイテムをページ上に単純に縦に配置します。</p>

<p>一番下にtapハンドラーが設定されたButtonを配置します。
ボタンが配置されているformパネルを返す、便利なupメソッドがあります。
そしてフォームのsubmitメソッドをコールし、上記で指定したURL(&#8216;contact.php&#8217;)に送信します。</p>

<h2>すべてをまとめましょう</h2>

<p>ここで別々に作成したビューを一緒にまとめてアプリケーションを完成させましょう。</p>

<div>[js]
    //We&#8217;ve added a third and final item to our tab panel &#8211; scroll down to see it
    Ext.application({
        name: &#8216;Sencha&#8217;,

        launch: function() {
            Ext.create(&quot;Ext.tab.Panel&quot;, {
                fullscreen: true,
                tabBarPosition: &#8216;bottom&#8217;,

                items: [
                    {
                        title: &#8216;Home&#8217;,
                        iconCls: &#8216;home&#8217;,
                        cls: &#8216;home&#8217;,
                        html: [
                            &#8216;&lt;img width=&quot;65%&quot; src=&quot;http://staging.sencha.com/img/sencha.png&quot; /&gt;&#8217;,
                            &#8216;&lt;h1&gt;Welcome to Sencha Touch&lt;/h1&gt;&#8217;,
                            &quot;&lt;p&gt;You&#8217;re creating the Getting Started app. This demonstrates how &quot;,
                            &quot;to use tabs, lists and forms to create a simple app&lt;/p&gt;&quot;,
                            &#8216;&lt;h2&gt;Sencha Touch 2&lt;/h2&gt;&#8217;
                        ].join(&quot;&quot;)
                    },
                    {
                        xtype: &#8216;nestedlist&#8217;,
                        title: &#8216;Blog&#8217;,
                        iconCls: &#8216;star&#8217;,
                        displayField: &#8216;title&#8217;,

                        store: {
                            type: &#8216;tree&#8217;,

                            fields: [
                                &#8216;title&#8217;, &#8216;link&#8217;, &#8216;author&#8217;, &#8216;contentSnippet&#8217;, &#8216;content&#8217;,
                                {name: &#8216;leaf&#8217;, defaultValue: true}
                            ],

                            root: {
                                leaf: false
                            },

                            proxy: {
                                type: &#8216;jsonp&#8217;,
                                url: &#8216;https://ajax.googleapis.com/ajax/services/feed/load?v=1.0&amp;q=http://feeds.feedburner.com/SenchaBlog&#8217;,
                                reader: {
                                    type: &#8216;json&#8217;,
                                    rootProperty: &#8216;responseData.feed.entries&#8217;
                                }
                            }
                        },

                        detailCard: {
                            xtype: &#8216;panel&#8217;,
                            scrollable: true,
                            styleHtmlContent: true
                        },

                        listeners: {
                            itemtap: function(nestedList, list, index, element, post) {
                                this.getDetailCard().setHtml(post.get(&#8216;content&#8217;));
                            }
                        }
                    },
                    //this is the new item
                    {
                        title: &#8216;Contact&#8217;,
                        iconCls: &#8216;user&#8217;,
                        xtype: &#8216;formpanel&#8217;,
                        url: &#8216;contact.php&#8217;,
                        layout: &#8216;vbox&#8217;,

                        items: [
                            {
                                xtype: &#8216;fieldset&#8217;,
                                title: &#8216;Contact Us&#8217;,
                                instructions: &#8216;(email address is optional)&#8217;,
                                items: [
                                    {
                                        xtype: &#8216;textfield&#8217;,
                                        label: &#8216;Name&#8217;
                                    },
                                    {
                                        xtype: &#8216;emailfield&#8217;,
                                        label: &#8216;Email&#8217;
                                    },
                                    {
                                        xtype: &#8216;textareafield&#8217;,
                                        label: &#8216;Message&#8217;
                                    }
                                ]
                            },
                            {
                                xtype: &#8216;button&#8217;,
                                text: &#8216;Send&#8217;,
                                ui: &#8216;confirm&#8217;,
                                handler: function() {
                                    this.up(&#8216;formpanel&#8217;).submit();
                                }
                            }
                        ]
                    }
                ]
            });
        }
    });
[/js]</div>

<p>このガイドの全てのソースコードは、ダウンロードしたSenca Touch 2.0 SDK の examples/getting_started にあります。</p>
]]></content:encoded>
					
		
		
			</item>
		<item>
		<title>Getting Started with Sencha Touch 2</title>
		<link>/514</link>
		
		<dc:creator><![CDATA[sunvisor]]></dc:creator>
		<pubDate>Fri, 02 Mar 2012 06:29:42 +0000</pubDate>
				<category><![CDATA[Sencha Touch]]></category>
		<guid isPermaLink="false">/?p=514</guid>

					<description><![CDATA[Sencha Touch 2.0 のRCが公開されています。 そのドキュメントのガイドにある Getting Started with Sencha Touch 2 を翻訳しました。 Sencha Touch 2 をはじ [&#8230;]]]></description>
										<content:encoded><![CDATA[<p>Sencha Touch 2.0 のRCが公開されています。
そのドキュメントのガイドにある Getting Started with Sencha Touch 2 を翻訳しました。
<span id="more-514"></span></p>

<h1>Sencha Touch 2 をはじめよう</h1>

<h2>Sencha Touch とは?</h2>

<p>Sencha Touch を使うと素早く簡単に、AndroidやiOS、Blackberryで動作する
HTML5ベースのモバイルアプリケーションを作成でき、
ネイティブアプリケーションのような操作性をブラウザーのナックで実現できます。</p>

<h2>必要なもの</h2>

<p>最初に必要なものは次のとおりです。</p>

<ul>
<li>フリーの<a href="http://www.sencha.com/products/touch/download/">Sencha Touch 2.0 SDK</a></li>
<li>ローカルで動作するWebサーバー</li>
<li>モダンWebブラウザー。ChromeまたはSafariを推奨します。</li>
</ul>

<p>SDKの最新バージョンをダウンロードして解凍します。
解凍したフォルダーをWebサーバーのドキュメントルートに配置します。
もしローカルにWebサーバーがインストールされていないのでしたら、
<a href="http://www.wampserver.com/en/">WAMP</a> や <a href="http://www.mamp.info/en/index.html">MAMP</a>
のようなワンクリックでインストールできるものを推奨します。</p>

<p>正しい場所にフォルダーを配置したら、Webブラウザーを開いて、
http://localhost/sencha-touch-folder
(Webサーバー公開するように設定された場所)
を開くと、Sencha Touch のウェルカムページが表示されます。</p>

<p>これで、最初のアプリケーションを作成する準備ができました。</p>

<h2>アプリケーションを作り始める</h2>

<p>Sencha Touch のアプリケーションは次のような簡単なアプリケーション構造ガイドラインに従うとうまく動きます。
これは簡単に保守しやすいアプリケーションを書くための規約とクラスのセットで、特にチームの一員として仕事をするときに役に立ちます。</p>

<p>最初のステップはアプリケーションを格納する簡単なフォルダー構造をセットアップすることです。
最初に必要なのは2つのファイルとSencha Touchのコピーです。
規約では次のようになります。</p>

<ul>
<li><strong>index.html</strong> &#8211; Sencha Touch とアプリケーションのファイルをインクルードする簡単なHTMLファイル。</li>
<li><strong>app.js</strong> &#8211; アプリケーション名、ホームスクリーンアイコン、起動時に実行することを定義するファイル。</li>
<li><strong>touch</strong> &#8211; ダウンロードしたSencha Touchフォルダー。</li>
</ul>

<p>index.htmlファイルから始めましょう。次のようになります。</p>

<div>[html]
    &lt;!DOCTYPE html&gt;
    &lt;html&gt;
    &lt;head&gt;
        &lt;title&gt;Getting Started&lt;/title&gt;
        &lt;link rel=&quot;stylesheet&quot; href=&quot;touch/resources/css/sencha-touch.css&quot; type=&quot;text/css&quot;&gt;
        &lt;script type=&quot;text/javascript&quot; src=&quot;touch/builds/sencha-touch-all-debug.js&quot;&gt;&lt;/script&gt;
        &lt;script type=&quot;text/javascript&quot; src=&quot;app.js&quot;&gt;&lt;/script&gt;
    &lt;/head&gt;
    &lt;body&gt;&lt;/body&gt;
    &lt;/html&gt;
[/html]</div>

<p>おそらくこれまで書いた中で最も簡単なHTMLファイルじゃないでしょうか。
やっていることは、Sencha Touch(Javascriptファイルとスタイルシート)とapp.jsファイルをインクルードしているだけです。
ボディが空っぽですね。Sencha Touchに描画させましょう。</p>

<p>次は、app.jsファイルの中身を見てみましょう。
ここでは簡単に、動作を確認するために &#8216;alart&#8217; をコールしているだけです。</p>

<div>[js]
    Ext.application({
        name: &#8216;Sencha&#8217;,

        launch: function() {
            alert(&#8216;launched&#8217;);
        }
    });
[/js]</div>

<p>以上で終わりです。
では、SafariかChromeを起動してちゃんと動作するか確認してみましょう。
動かしてみると、アラートメッセージが表示されます。
(本家のガイドでは Live Preview をクリックすると実行できるようになっています)
別にどうってことはありませんが、アラートメッセージがポップアップしたと言うことはSencha Touchがちゃんと動作して起動したと言うことです。</p>

<p>最後に伝統的なHello Worldを表示するPanelを作ってみます。</p>

<p>これも非常に簡単。次のようにlaunch関数をExt.createを使うように変更するだけです。</p>

<div>[js]
    Ext.application({
        name: &#8216;Sencha&#8217;,

        launch: function() {
            Ext.create(&#8216;Ext.Panel&#8217;, {
                fullscreen: true,
                html: &#8216;Hello World&#8217;
            });
        }
    });
[/js]</div>

<h2>次のステップ</h2>

<p>これで簡単なページとHello Worldを作成できました。次は最初の簡単なアプリケーションを作ります。
次のステップは
<a href="http://docs.sencha.com/touch/2-0/#!/guide/first_app">First Application guide</a>
<a href="518">翻訳はこちら</a>
になります。
ここでは、簡単ですがパワフルなアプリケーションを15分ほどで作り上げます。</p>

<p>次のステップに進むかフレームワークの他の側面について詳細を知りたい場合は次のガイドやリソースをチェックすることをお勧めします。</p>

<h3>ガイド</h3>

<ul>
<li><a href="http://docs.sencha.com/touch/2-0/#!/guide/components">Components and Containers</a></li>
<li><a href="http://docs.sencha.com/touch/2-0/#!/guide/apps_intro">Intro to Applications</a></li>
<li><a href="http://docs.sencha.com/touch/2-0/#!/guide/layouts">The Layout System</a></li>
<li><a href="http://docs.sencha.com/touch/2-0/#!/guide/data">The Data Package</a></li>
<li><a href="http://docs.sencha.com/touch/2-0/#!/guide/whats_new">What&#8217;s New in Sencha Touch 2</a></li>
</ul>

<h3>アプリケーションサンプル</h3>

<ul>
<li><a href="http://docs.sencha.com/touch/2-0/#!/example/kitchen-sink">Kitchen Sink</a></li>
<li><a href="http://docs.sencha.com/touch/2-0/#!/example/twitter">Twitter</a></li>
<li><a href="http://docs.sencha.com/touch/2-0/#!/example/kiva">Kiva</a></li>
</ul>

<h3>コンポーネントサンプル</h3>

<ul>
<li><a href="http://docs.sencha.com/touch/2-0/#!/example/carousel">Carousel</a></li>
<li><a href="http://docs.sencha.com/touch/2-0/#!/example/forms">Forms</a></li>
<li><a href="http://docs.sencha.com/touch/2-0/#!/example/pickers">Date Picker</a></li>
</ul>
]]></content:encoded>
					
		
		
			</item>
	</channel>
</rss>
