<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"
	xmlns:content="http://purl.org/rss/1.0/modules/content/"
	xmlns:wfw="http://wellformedweb.org/CommentAPI/"
	xmlns:dc="http://purl.org/dc/elements/1.1/"
	xmlns:atom="http://www.w3.org/2005/Atom"
	xmlns:sy="http://purl.org/rss/1.0/modules/syndication/"
	xmlns:slash="http://purl.org/rss/1.0/modules/slash/"
	>

<channel>
	<title>Ext3 &#8211; Sunvisor Lab. Ext JS 別館</title>
	<atom:link href="/tag/ext3/feed" rel="self" type="application/rss+xml" />
	<link>/</link>
	<description>Ext JS 関連の話題を取り上げます。</description>
	<lastBuildDate>Sat, 03 Mar 2012 07:37:25 +0000</lastBuildDate>
	<language>ja</language>
	<sy:updatePeriod>
	hourly	</sy:updatePeriod>
	<sy:updateFrequency>
	1	</sy:updateFrequency>
	<generator>https://wordpress.org/?v=6.1</generator>
	<item>
		<title>EditorGridPanel 二題</title>
		<link>/222</link>
		
		<dc:creator><![CDATA[sunvisor]]></dc:creator>
		<pubDate>Wed, 08 Jun 2011 05:38:40 +0000</pubDate>
				<category><![CDATA[Ext JS]]></category>
		<category><![CDATA[Grid]]></category>
		<category><![CDATA[EditorGridPanel]]></category>
		<category><![CDATA[Ext3]]></category>
		<guid isPermaLink="false">/?p=222</guid>

					<description><![CDATA[本館のブログで，EditorGridPanlでEnterキーの動作を変更する &#124; Sunvisor Lab. という記事を書きました。EditorGridPanel での Enterキーの挙動を変更するというエントリーで [&#8230;]]]></description>
										<content:encoded><![CDATA[<p>本館のブログで，<a href="http://www.sunvisor.net/node/409">EditorGridPanlでEnterキーの動作を変更する | Sunvisor Lab.</a> という記事を書きました。EditorGridPanel での Enterキーの挙動を変更するというエントリーです。</p>

<p>その件に関する追加情報と，EditorGridPanelで編集モードに入ったときに，項目を選択状態にする件についてです。</p>

<p><span id="more-222"></span></p>

<h2>続）EditorGridPanel の Enter キーの動作を変更する</h2>

<p>そのエントリーの中では，インスタンス化された EditorGridPanel のメソッドを再定義することで思ったような挙動をさせることをしていました。この方法では，あるEditrotGridPanelに対してのみ効果を発揮します。他のところで使用されているEditrotGridPanelは以前の挙動のままです。では，アプリケーション全体でEditrotGridPanelの挙動を変えたい場合はどうすればよいでしょうか。</p>

<p>インスタンス化される前に，メソッドを入れ替えてやります。クラス自体の定義を変えてやれば，その後インスタンス化したもの全体に影響しますからね。この方法でメソッドを入れ替えるには，Ext.override メソッドを使います。</p>

<div>
[js]
Ext.override(Ext.grid.CellSelectionModel, {

    onEditorKey: function(field, e){

        var k = e.getKey(),
                g = this.grid,
                ed = g.activeEditor,
                newCell,
                me = this;

        if( k == e.TAB ){
            me.handleKeyDown(e);
        }else if( k == e.UP || k == e.DOWN || k == e.LEFT || k == e.RIGHT ){
            if( ed ) ed.completeEdit();
            me.handleKeyDown(e);
        }else if( k == e.ENTER){
            var s = this.selection;
            var r = s.cell[0], c = s.cell[1];
            newCell = g.walkCells(r + 1, c, 1, this.isSelectable, this);
            if( newCell ){
                me.select(newCell[0], newCell[1]);
                if( ed ) ed.completeEdit();
                me.handleKeyDown(e);
            }
        }
    }
});
[/js]
</div>

<p>なんだか最初の方法よりもスマートです。こっちで行くことにしましょう。</p>

<p>でも，全部のEditrotGridPanelでこの動作は必要ないけど，いくつかのところでいるんだよねってときはどうしましょう。インスタンスごとに新しい挙動をオン・オフできるようにしたいということです。</p>

<p>そんな時は，プラグインを使いうんだそうです。プラグインを使えば，あるコンポーネントに機能を追加できる上に，その動きを制御することもできるんだって。さらにスマートなイメージがします。もうちょっと勉強して使えるようになろう。そう言っている内にExt JS 4の時代になるなきっと。</p>

<h2>編集モードに入ったときに全部選択しておいて</h2>

<p>上記のやり方でEnterキーの動作を変更したグリッドを使ってもらっていたら，</p>

<p>「Enterで値の訂正に入ったときに，選択状態が何も選択されていない状態だから，入力する前に古い値を一生懸命削除しなきゃならんのがめんどうなんだよね。編集モードに入ったら，全て選択されている状態にはならんもんなん？」</p>

<p>という要望をうけました。前の記事でも目的として書いていたように</p>

<pre><code>&lt;数値&gt;&lt;Enter&gt;&lt;数値&gt;&lt;Enter&gt;&lt;数値&gt;&lt;Enter&gt;&lt;数値&gt;&lt;Enter&gt;&lt;数値&gt;&lt;Enter&gt;
</code></pre>

<p>という入力ができるのが目的ですから，この要望は当然です。</p>

<p>これの解決は意外と簡単でした。ColumnModelのコンフィグで，次のように selectOnFocus コンフィグを
true にするだけです。</p>

<div>
[js]
    {
        xtype: &#8216;gridcolumn&#8217;,
        dataIndex: &#8216;money1&#8217;,
        header: &#8216;金額1&#8217;,
        sortable: false,
        width: 100,
        editor: {
            xtype: &#8216;numberfield&#8217;,
            selectOnFocus: true
        }
    },
[/js]
</div>
]]></content:encoded>
					
		
		
			</item>
		<item>
		<title>Gridで合計行を表示</title>
		<link>/168</link>
					<comments>/168#comments</comments>
		
		<dc:creator><![CDATA[sunvisor]]></dc:creator>
		<pubDate>Mon, 16 May 2011 05:39:31 +0000</pubDate>
				<category><![CDATA[Ext JS]]></category>
		<category><![CDATA[Grid]]></category>
		<category><![CDATA[Ext3]]></category>
		<guid isPermaLink="false">/?p=168</guid>

					<description><![CDATA[参考 こちらのサイトの記事を参考にさせてもらいました。Gridでデータを入力できるようにしていたら，「合計を表示できないか」といわれまして，なんとかそれを実現すべく考えました。対象はExt3です。 合計行を表示するには， [&#8230;]]]></description>
										<content:encoded><![CDATA[<p><dt>参考</dt> <dd> <a href="http://www.terrasky.co.jp/blog/jimaoka/2009/12/extjs.html">こちらのサイト</a>の記事を参考にさせてもらいました。</dd>Gridでデータを入力できるようにしていたら，「合計を表示できないか」といわれまして，なんとかそれを実現すべく考えました。対象はExt3です。<span id="more-168"></span></p>

<p>合計行を表示するには，グリッドに合計行を加えるか，合計用のグリッドを用意するかですが，縦スクロールしても合計行が画面からはみ出ないようにしたいので，合計用のグリッドを用意することにしました。合計行を表示させるためにやらなければならないことは次のとおりです。</p>

<ol>
    <li>明細グリッドと合計グリッドの横スクロールを同期させる。</li>
    <li>明細グリッドの横スクロールバーはいらないので表示させない。</li>
    <li>明細グリッドで列幅を変更したら合計グリッドの列幅も変更する。</li>
    <li>明細グリッドで列を移動したら合計グリッドの列も移動する。</li>
    <li>合計の計算をする。</li>
</ol>

<p>順番に解決していきましょう。とその前に，完成時のスクリーンショットを載せておきます。</p>

<p><a href="/wp-content/uploads/2011/05/totalgrid.png"><img decoding="async" loading="lazy" class="alignnone size-full wp-image-173" title="totalgrid" src="/wp-content/uploads/2011/05/totalgrid.png" alt="" width="562" height="242" srcset="/wp-content/uploads/2011/05/totalgrid.png 703w, /wp-content/uploads/2011/05/totalgrid-300x129.png 300w" sizes="(max-width: 562px) 100vw, 562px" /></a></p>

<p>と，こんな感じになります。数値列はちゃんと右寄せしろよ！って感じですが。</p>

<h2>横スクロールを同期させる</h2>

<p>横スクロールバーは合計グリッドにのみ存在しますので，合計グリッドのスクロールが発生したら，明細グリッドをスクロールさせるだけでいいような感じがしますが，明細グリッドの入力時にキー入力によって明細グリッドにスクロールが起こることがあるので，双方のグリッドのスクロールイベントをリッスンして同期を取る必要があります</p>

<div>[js]
    onSumScroll: function( left, top ){

        var me = this;

        me.syncScroll( me.grdData, left, top );
    },

    onDataScroll: function( left, top ){

        var me = this;

        me.syncScroll( me.grdSum, left, top );
    },

    syncScroll: function( tgt, left, top ){
        var me = this;
        var con = tgt.getEl().child(&#8216;*[class*=x-grid3-scroller]&#8217;);

        con.scrollTo( &quot;top&quot;, top, false );
        con.scrollTo( &quot;left&quot;, left, false );

    },
[/js]</div>

<p>参考サイトでは，ExtJS2なのでscrollToの引数が違っています。こちらはExt3のコードです。onSumScroll を 合計グリッドの bodyscroll イベントにセットし，onDataScroll を 明細グリッドの bodyscroll イベントにセットします。</p>

<h2>明細グリッドの横スクロールバーを消す</h2>

<p>スクリーンショットのように明細グリッドと合計グリッドは上下に並べて表示させます。二つのグリッドは同期してスクロールされるので，明細グリッドには横スクロールバーは不要です。それを表示させないようにします。</p>

<div>[js]
    onAfterRender: function() {

        var me = this;
        var el = me.grdData.getEl().child(&#8216;*[class*=x-grid3-scroller]&#8217;);

        el.setStyle(&#8216;overflow-x&#8217;, &#8216;hidden&#8217;);
    },
[/js]</div>

<p>これはフォームのafterrenderイベントのコードです。ここで明細グリッドの横スクロールバーを表示させないように，エレメントのoverflow-xをfalseに変更しています。</p>

<h2>列幅の変更も同期</h2>

<p>列幅が変更されたときに，合計グリッドの列幅も変更します。</p>

<div>[js]
    syncColumnResize: function( columnIndex, newSize) {
        var me = this;

        me.grdSum.colModel.setColumnWidth(columnIndex, newSize);

    },
[/js]</div>

<p>明細グリッドの <code>columnresize</code> イベントで上記のメソッドがコールされるようにします。単にそのまま <code>setColumnWidth</code> メソッドをコールしているだけです。</p>

<h2>列の移動も同期</h2>

<p>同様に列の移動を同期しようと，</p>

<div>[js]
    syncColumnMove: function( columnIndex, newIndex ){
        var me = this;

        me.grdSum.colModel.moveColumn(columnIndex, newIndex);

    },
[/js]</div>

<p><del>と書いてみましたが，なぜか動きません。そしてこのメソッドが動いた後，なんだか動きが編になります。(´･ω･`) 現在調査中。</del></p>

<p>うまく動作しない理由がわかりました。明細・合計それぞれのcolModelをnewするときにコンフィグを指定しますが，両方とも同じ構造ですから，一つの配列を渡していました。それでおかしくなっちゃっていたのです。コンフィグの配列を別のものにするとちゃんと動きました。別な勉強をしました。</p>

<h2>合計の計算をする</h2>

<p>合計行ということですからデータから合計を計算しなければ行けません。1つの列の合計をとるメソッドを用意。</p>

<div>[js]
    calcSum: function(colName){

        var me = this;
        var dataStore = me.grdData.getStore(),
            sumStore = me.grdSum.getStore();
        var dataRec,
            sumRec = sumStore.getAt(0),
            row, val=0;

        for( row = 0; row &lt; dataStore.getCount(); row++ ){
            dataRec = dataStore.getAt( row );
            val += dataRec.get(colName);
        }
        sumRec.set(colName, val);
        sumStore.commitChanges();
    },
[/js]</div>

<p>まず，明細ストアのデータがロードされたときに全コラムを計算します。</p>

<div>[js]
        me.store.load({
            callback: function () {

                me.calcSum(&#8216;money1&#8217;);
                me.calcSum(&#8216;money2&#8217;);
                me.calcSum(&#8216;money3&#8217;);
                me.calcSum(&#8216;money4&#8217;);
                me.calcSum(&#8216;money5&#8217;);

            }
        });
[/js]</div>

<p>コラムが多い場合はもっとスマートな書き方を考えましょうね。あとは明細グリッドが変更されたときに対象となるコラムだけを再計算させます。</p>

<div>[js]
    onAfterEdit: function(e){

        var me =this,
            fld = e.field;

        me.calcSum(fld);
    },
[/js]</div>

<p>この場合は，EditorGridPanel のafteredit イベントにセットしています。</p>
]]></content:encoded>
					
					<wfw:commentRss>/168/feed</wfw:commentRss>
			<slash:comments>1</slash:comments>
		
		
			</item>
		<item>
		<title>サーバーサイドでDirectStoreの更新処理を書く</title>
		<link>/127</link>
		
		<dc:creator><![CDATA[sunvisor]]></dc:creator>
		<pubDate>Mon, 28 Mar 2011 07:59:18 +0000</pubDate>
				<category><![CDATA[Direct]]></category>
		<category><![CDATA[Ext JS]]></category>
		<category><![CDATA[xFrameworkPX]]></category>
		<category><![CDATA[DirectStore]]></category>
		<category><![CDATA[Ext3]]></category>
		<guid isPermaLink="false">/?p=127</guid>

					<description><![CDATA[08.で DirectStore でのサーバー更新について調べましたが，それでは実際にサーバー側のルーチンはどう書いたらいいのでしょうか。ここでは，PHPとxFrameworkPXを使った場合を例示します。xFramew [&#8230;]]]></description>
										<content:encoded><![CDATA[<p>08.で DirectStore でのサーバー更新について調べましたが，それでは実際にサーバー側のルーチンはどう書いたらいいのでしょうか。ここでは，PHPとxFrameworkPXを使った場合を例示します。xFrameworkPXでは，ExtDirectコントローラーを使えば，モジュールのメソッドがそのままExtDirectで使えるという便利な機能があります。ここでは，そのモジュールのメソッドの記述例を示してExtDirectでのサーバー側の実装を示します。ただ，他のフレームワークを使った場合でも，API周りの処理に違いがあるとは思いますが，実際に処理するメソッド自体は大同小異だと思いますので，参考にはなると思います。<span id="more-127"></span></p>

<h2>クライアント側 JavaScript</h2>

<h3>DirectStore の定義</h3>

<p>次のような DirectStore を定義して，このストアの save メソッドをコールしたときに，ちゃんとサーバー側で更新がかかるようにします。例では autoSave を false にして自動保存されないようにしていますが，自動保存する場合でもほぼ同じです。この例では，保存するときの beforewrite イベントでローディングマスクを表示する処理を加えて， write イベントでマスクを解除しています。API コンフィグで指定しているのは，ExtDirect で公開されているメソッドです。Ext.remote というネームスペースに定義されるようになっています。CRUDそれぞれのメソッドが定義されています。</p>

<ul>
<li>Create = insertDataメソッド</li>
<li>Read = readDataメソッド</li>
<li>Update = updateData メソッド</li>
<li>Delete = deleteData メソッド</li>
</ul>

<h3>Store へのレコードの追加</h3>

<p>ここでは参考までに Store にデータを追加するコードを紹介します。</p>

<div>[js]
var newRec;

newRec = new me.store.recordType(obj, null);
me.store.add( newRec );
[/js]</div>

<p>このように，recordType でレコードを作り，add メソッドで追加します。上記の例ではobjに追加したいレコードのデータが入っているという前提です。レコードの id プロパティが空の時に ExtJS はそれが追加されたレコードであると判断します。</p>

<h2>サーバー側 PHP(xFrameworkPX)</h2>

<p>次にサーバー側のコードを書いていきます。</p>

<h3>ExtDirect コントローラー</h3>

<p>.extdirect.php を webapp フォルダに作成します。</p>

<div>[php]
class extdirect extends xFrameworkPX_Controller_ExtDirect
{

    public $direct = array(
    &#8216;namespace&#8217; =&amp;gt; &#8216;Ext.remote&#8217;
    );
    public $modules = array(
    &#8216;employee&#8217; =&amp;gt; array( &#8216;conn&#8217; =&amp;gt; &#8216;default&#8217; )
    );

}
[/php]</div>

<p>ネームスペースを設定して，利用するモジュール(employee)をuseしています。これだけで xFrameworkPX がいろいろと世話をしてくれるので， employee モジュールのメソッドを ExtDirect から利用可能になります。</p>

<h3>employee モジュール</h3>

<div>[php]
class employee extends xFrameworkPX_Model
{

    public $usetable = &#8216;employee&#8217;;

    // メソッドを記述

}
[/php]</div>

<p>xFrameworkPX でのモジュールは，modules ディレクトリに配置します。</p>

<h3>readData メソッド</h3>

<p>データを読み出すための readData メソッドを実装します。</p>

<div>[php]
public function readData()
    {

        $fields = array(
            &#8216;id&#8217;,
            &#8216;name_kanji&#8217;,
            &#8216;name_kana&#8217;,
            &#8216;birth_date&#8217;
        );
        $order = array(
            &#8216;id&#8217;
        );

        $result = $this-&amp;gt;get(
            &#8216;all&#8217;,
            array(
                &#8216;fields&#8217; =&amp;gt; $fields,
                &#8216;order&#8217; =&amp;gt; $order
            )
        );

        return array(
            &#8216;success&#8217; =&amp;gt; true,
            &#8216;total&#8217; =&amp;gt; count( $result ),
            &#8216;items&#8217; =&amp;gt; $result
        );

    }
[/php]</div>

<p>データベースから従業員データをとってきて，その一覧を返しています。返却するデータは最後の return 文のような形式で返します。</p>

<p>この中の items というキー名は任意ですが，このキー名を Store の root コンフィグにセットしなければなりません。他のキー名にした場合には，Store の root コンフィグにその名前をセットします。</p>

<h3>deleteData メソッド</h3>

<p>destroy アクションが発行された場合に実行されます。この場合，サーバに渡されるオブジェクトの items プロパティに削除すべきレコードの id が渡されます。削除するレコードが複数ある場合には items は配列になります。この例での items となっている要素ですが，この items というキーは前述の通り， root コンフィグに設定されている値になります。root コンフィグの値が違っていればこのキー名も変わります。（この点については他のメソッドでも同様です）</p>

<p>実際の deleteData メソッドのコードです。</p>

<pre><code>&lt;div&gt;[php]
public function deleteData( $pData )
{

    $data = $pData-&amp;amp;gt;items;
    if( ! is_array($data) ){
        $data = array($data);
    }

    $result = true;

    foreach( $data as $id ){
        $result = $result &amp;amp;gt;&amp;amp;gt; $this-&amp;amp;gt;_deleteOne( $id );
    }
    return array(
        'success' =&amp;amp;gt; $result,
        'items' =&amp;amp;gt; $data
    );

}
</code></pre>

<p>    private function _deleteOne( $id )
    {</p>

<pre><code>    $where = array(
        'id = :id'
    );
    $bind = array(
        'id' =&amp;amp;gt; $id
    );
    $result = $this-&amp;amp;gt;delete(
        array(
            'where' =&amp;amp;gt; $where,
            'bind' =&amp;amp;gt; $bind
        )
    );
    return $result;

}
</code></pre>

<p>[/php]</div></p>

<p>サーバーに渡されるのはJSONのデータですが，PHPで受け取ったときには StdObject型のデータになります。配列の場合は配列で受け取れるのですが，オブジェクトの場合は StdObject のデータが渡されます。そこで最初に渡されたデータを配列にキャストしています。</p>

<p>Store の中で一つのレコードが削除された場合と，複数のレコードが削除された場合で，渡されるデータが違います。複数の場合のみデータは配列になっています。渡されたデータが配列であるかどうか判断して，配列でない場合は要素が一つの配列に格納してます。そしてその後，配列の各要素を引数にして _deleteOne メソッドをコールしています。</p>

<p>このメソッドの戻り値ですが，次のような内容を返す必要があります。</p>

<pre>{ success: &lt;flag>, items: [ {id : &lt;id1>}, {id: &lt;id2>}, ....] }</pre>

<p>success プロパティには実行結果をセットします。true の場合は成功，false の場合は失敗ですね。成功した場合には，items （例によって root コンフィグの値によって変わります）プロパティに，削除されたレコードの id フィールドの値をプロパティに持つオブジェクトの配列をセットします。戻り値の方は1件の削除でも配列にする必要があります。_deleteOne メソッドの最後の方でこの1件ごとのオブジェクトを作成して返しています。deleteData メソッドの最後では，それらの結果の配列と success プロパティをつけて返しています。</p>

<p>渡されるオブジェクトと返すオブジェクトの形式は，update や insert でもほぼ同じですので，よく覚えておいてください。</p>

<h3>updateData メソッド</h3>

<p>update アクションの時には，更新されたレコードのデータが渡されます。ここでも1レコードの場合は1件のオブジェクトが，複数レコードの場合はオブジェクトの配列が渡されます。Store の writeAllFields コンフィグの値によって，渡されるオブジェクトの内容が変わります。この値が true の場合は，すべてのフィールドが渡されますし，この値が false の場合はキーのフィールドと変更があったフィールドだけが渡されます。</p>

<div>[php]
public function updateData( $pData )
    {

        $data = $pData-&amp;gt;items;
        if( ! is_array($data) ){
            $data = array($data);
        }
        $result = true;
        foreach( $data as $rec ){
            $result = $result &amp;gt;&amp;gt; $this-&amp;gt;_updateOne( $rec );
        }

        return array(
            &#8216;success&#8217; =&amp;gt; $result,
            &#8216;items&#8217; =&amp;gt; $data
        );

    }

    private function _updateOne( $pRec )
    {

        $rec = (array)$pRec;

        $fields = array(
            &#8216;name_kanji&#8217;,
            &#8216;name_kana&#8217;,
            &#8216;birth_date&#8217;
        );
        $values = array(
            &#8216;:name_kanji&#8217;,
            &#8216;:name_kana&#8217;,
            &#8216;:birth_date&#8217;
        );
        $where = array(
            &#8216;id = :id&#8217;
        );

        return $this-&amp;gt;update(
            array(
                &#8216;field&#8217; =&amp;gt; $fields,
                &#8216;value&#8217; =&amp;gt; $values,
                &#8216;where&#8217; =&amp;gt; $where,
                &#8216;bind&#8217; =&amp;gt; $rec
            )
        );

    }
[/php]</div>

<p>このメソッドでは渡されたデータをひもといて update メソッドでレコードの更新をかけています。ここでも適切な値を返さなければなりません。やはり success プロパティと items プロパティが必要です。</p>

<p>ToDo:（items が id さえあればいいのかどうか確認。writeAllFields が false の場合も検証）</p>

<h3>insertData メソッド</h3>

<p>create アクションの時には，追加されたレコードのデータが渡されます。ここでも1レコードの場合は1件のオブジェクトが，複数レコードの場合はオブジェクトの配列が渡されます。戻り値も同様です。</p>

<div>[php]
public function addData( $pData )
    {

        $data = $pData-&amp;gt;items;
        if( ! is_array($data) ){
            $data = array($data);
        }

        $result = true;
        $rData = array();
        foreach( $data as $rec ){
            $rRec = (array)$rec;
            $id = $this-&amp;gt;_addOne( $rRec );
            $rRec[&#8216;id&#8217;] = $id;
            $rData[] = $rRec;
        }
        return array(
            &#8216;success&#8217; =&amp;gt; $result,
            &#8216;items&#8217; =&amp;gt; $rData
        );

    }

    private function _addOne( $rec )
    {

        $fields = array(
            &#8216;name_kanji&#8217;,
            &#8216;name_kana&#8217;,
            &#8216;birth_date&#8217;
        );
        $values = array(
            &#8216;:name_kanji&#8217;,
            &#8216;:name_kana&#8217;,
            &#8216;:birth_date&#8217;
        );
        // 挿入実行
        $result = $this-&amp;gt;insert(
            array(
                 &#8216;field&#8217; =&amp;gt; $fields,
                 &#8216;value&#8217; =&amp;gt; $values,
                 &#8216;bind&#8217; =&amp;gt; $rec
            )
        );
        $id = $this-&amp;gt;lastId();

        return $id;

    }
[/php]</div>

<p>このメソッドでは，id フィールドは auto increment になっているという前提です。</p>

<p>ここのコードは，記事に書くためにちょっと短くしています。<a href="https://github.com/sunvisor/DirectStoreSample">こちらのGitHubリポジトリ</a>ですべてのソースをご覧になれます。</p>
]]></content:encoded>
					
		
		
			</item>
		<item>
		<title>Ext Direct での更新処理</title>
		<link>/36</link>
		
		<dc:creator><![CDATA[sunvisor]]></dc:creator>
		<pubDate>Mon, 24 Jan 2011 05:05:54 +0000</pubDate>
				<category><![CDATA[Direct]]></category>
		<category><![CDATA[Ext JS]]></category>
		<category><![CDATA[xFrameworkPX]]></category>
		<category><![CDATA[Ext3]]></category>
		<guid isPermaLink="false">http://wp.sunvisor.net/?p=36</guid>

					<description><![CDATA[Ext Directでの更新処理をやってみたいと思いました。やっぱりExtJSらしいところで，GridPanel（いや更新ですからEditorGridPanelか）で訂正したデータをサーバーに更新をかけるということになり [&#8230;]]]></description>
										<content:encoded><![CDATA[<p>Ext Directでの更新処理をやってみたいと思いました。やっぱりExtJSらしいところで，GridPanel（いや更新ですからEditorGridPanelか）で訂正したデータをサーバーに更新をかけるということになります。</p>

<p>サーバーサイドのxFrameworkPXのモデルに更新用のメソッドを用意して，DirectStoreのapiにそれをセットするのかなと思ったのですが，apiの仕様がいまいちよくわかりません。パラメータの渡し方も。ですので，更新をStoreに任せるのではなくて，違う方法での実現を考えました。<span id="more-36"></span></p>

<p>※ その後，勉強の甲斐あって，Storeに任せる方法もわかりました。こちらを参照ください＞<a href="http://www.sunvisor.net/node/412">08.DirectStore でサーバー更新をかける</a></p>

<h2>JavaScript側での更新タイミング</h2>

<p>更新をかけるタイミングとしてまず注目したのは，Storeのupdateイベントです。データの更新があったらこのイベントが発生します。このイベントで更新をかけてはどうかと考えました。</p>

<p>EditorGridPanelに結びつけられたStoreでのupdateイベントは，Gridのセルが変更されたときに発火します。その際にoperationには，Ext.data.Record.EDITがセットされます。また，StoreのcommitChangesがコールされたときにも発火します。その際には，operationは，Ext.data.Record.COMMITが渡されます。また，このイベントにはStoreとRecordが渡されます。</p>

<p>operationがExt.data.Record.EDITの時のイベントでサーバーを更新する方法もありですが，すべての更新を終えて「保存」ボタンをクリックしたようなときに更新をかけたいと思います。ですので，「保存」ボタンをクリックしたらStoreのcommitChangesをコールして，updateイベントでサーバーを更新させようと考えました。</p>

<p>複数のレコードが更新されてCOMMITされた時に，どんな動作をするのでしょうか。更新されたレコード数だけupdateイベントが発生するのでしょうか。違います。一度だけしか発生しません。そのときのrecordは最後に更新されたレコードだけがセットされています。ではStoreのgetModifiedRecordメソッドで更新されたレコードを得て，それを対象に更新しましょう。とおもいきや，updateイベントが発生するのは，すでにStoreがコミットされた後みたいで，getModifiedRecordメソッドで返ってくるレコードは空っぽでした。</p>

<p>ですので，updateイベントで処理するのではなく，「保存」ボタンのclickイベントハンドラの中でStoreをサーバー側に転送する処理を書くことにします。</p>

<p>Storeの中での更新されたレコードを得るには，さきほどから出てきているgetModifiedRecordメソッドを使います。戻り値はrecordの配列です。さらにレコードの中の更新されたフィールドを得るには，recordのmodifiedプロパティで得られますよとAPIドキュメントに書いてあります。modifiedは変更されたフィールド名をキーにして変更前の値を値として持ちます。recordのdataプロパティとこのmodifiedプロパティを使えば，更新されたレコードとフィールドをサーバー側に伝えることができそうです。</p>

<p>と，いま，APIドキュメントを参照していたら，recordには，getChanges()というメソッドもあります。こちらはそのものずばりで，変更されたフィールド名と変更後の値のセットが得られるようです。そこで，サーバー側には，RecordのidとgetChanges()で得られたオブジェクトを渡したらいいんじゃないでしょうか。JavaScript側のソースです。Saveボタンが押されたときのイベントハンドラです。</p>

<div>[js]
    onBtnSaveClick: function(button, e) {
        var store = Ext.StoreMgr.get(&#8216;StaffStore&#8217;);
        var mrec = store.getModifiedRecords();
        var changed;
        var rowId;

        for(var r=0; r &amp;lt; mrec.length; r++){
            changed = mrec[r].getChanges();
            rowId = mrec[r].id;
            // サーバーサイド関数を呼び出す
            Staff.updateRec(rowId, changed);
        }
        store.commitChanges();
    },
[/js]</div>

<p>getModifiedRecordsで変更のあったレコードを取り出し，それぞれのレコードについて，サーバー側のメソッドupdateRecを呼び出しています。その際に渡しているのが，Record.idとgetChanges()の戻り値です。結構簡単なハンドラになりましたね。</p>

<p>サーバー側のモジュールのメソッドです。</p>

<div>[php]
public function updateRec($id, $changedData)
    {
        // 引数を連想配列にキャスト
        $updateData = (array)$changedData;
        // データの更新
        $this-&amp;gt;set(
            $updateData,
            array(&#8216;id&#8217; =&amp;gt; $id)
        );
    }
[/php]</div>

<p>xFrameworkPXのモジュールのsetメソッドは，更新するフィールド名と値のセットを渡すので，ExtJSのgetChanges()メソッドの戻り値がそのままんま使えます。だからメソッドは上記のように超簡単な記述で済みます。すごい！簡単！かっこいい！</p>

<p>なお，渡された$changeDataは，stdClassとして渡されるので，4行目で連想配列にキャストしています。</p>

<p>いろいろ試行錯誤しましたが，最終的には非常にスマートな感じで更新部分ができました。これもxFramewordPXのおかげですね。</p>
]]></content:encoded>
					
		
		
			</item>
	</channel>
</rss>
